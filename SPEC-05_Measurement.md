# SPEC-05 · Measurement Specification

U+유모바일 비로그인 모바일 메인 개편 · Development Specification
작성 2026-08-27 · 기준 커밋 `beea5bb`
선행 · `SPEC-02_Functional.md` · `SPEC-03_State_Exception.md` · `SPEC-04_Data_Handoff.md`

> **이 문서는 새 KPI나 새 이벤트를 만드는 문서가 아니다.** PHASE 2~7-4에서 확인된 관측 사실과 기존 ME/DV/OI 항목을 개발·분석이 사용할 수 있는 형태로 정리한다. 원자료에 없는 event name·parameter·KPI는 만들지 않는다.

---

## 01. 측정 범위와 경계

### 1-1. Prototype console log — 21종

프로토타입 `prototype.html` 의 `log()` 함수가 콘솔에 출력하는 문자열이다.

`INIT` · `RANGE_SELECT` · `HANDOFF` · `NAVIGATE` · `BACK` · `M03_STATE` · `M03_RETRY` · `M04_STATE` · `M05_STATE` · `PHONE_TOGGLE` · `PHONE_TAB` · `ACC_TOGGLE` · `CAROUSEL` · `SEARCH_OPEN` · `ATF_GUIDE` · `ANNOTATE` · `AUDIT_OK` · `AUDIT_FAIL` · `BLOCKED` · `RESET` · `RESIZE`

> **이 21종은 QA / 개발 검증용이며 운영 측정 이벤트가 아니다.**
> GA4/GTM 이벤트로 간주하지 않는다. 로그 이름을 운영 event name으로 승격하지 않는다.
> 상태: **`PROTOTYPE-ONLY`**

전체 대조표는 §04.

### 1-2. GA4 / GTM

운영 측정의 기준 시스템이다. 본 명세는 **원자료에서 확인된 범위까지만** 기술한다.

| 항목 | 확인된 내용 | 상태 |
|---|---|---|
| 관측 기간 | 2026-07-28 ~ 08-24 (일부 대조는 07-29 ~ 08-25) | `OBSERVED` |
| GTM 카테고리 | `MAIN_NOTLOGIN` (메인 본문) · `GNB` · `UTIL_TOP` · `메인_PC` | `OBSERVED` |
| GTM action (메인 본문) | `QuickMenu` · `ProductBanner` · `ProductProBanner` · `RecommendGroup` · `SearchFee` · `BenefitService` · `CustomerCenter` · `Banner` · `PopupTop` · `PopupClose` · `Popup_79` · `Popup_95` | `CONFIRMED` (PHASE 2에서 action ↔ 영역 매핑 확정) |
| 태깅 이벤트 총량 | 170,053 events · 그중 팝업 관련 133,059 (78.2%) | `OBSERVED` |
| event name · parameter 사양 | **원자료에 기록 없음** | **`UNVERIFIED`** |

> GTM **action 이름**은 확인됐으나, GA4 **event name과 parameter 사양**은 어느 원자료에도 없다. 일반적인 GA4 관행("`click`", "`module_id`" 등)을 이유로 만들지 않는다.

### 1-3. Clarity

| 항목 | 내용 | 상태 |
|---|---|---|
| 용도 | 스크롤 도달률 · 클릭 텍스트 · Dead/Rage click · Quick back · 세션 녹화 | `OBSERVED` |
| 주요 관측 | 스크롤 도달 44.5% / 39.2% · Quick back 54.8% / 39.3% · Dead click 2.4% / 1.5% · Rage click 0.1% / 0.0% | `OBSERVED` |
| 녹화 표본 | 300건 (부분 분석은 n=150 · 70/90 · 53/60 등 소표본) | `OBSERVED` |
| **한계 1** | 모수가 원자료마다 다르다 → §03 | **`UNVERIFIED`** |
| **한계 2** | 스크롤 지표의 정의("최대 도달 위치 ÷ 페이지 높이")가 **가정**이다 | **`UNVERIFIED`** · DV-07 |
| **한계 3** | 클릭 텍스트 집계 필터가 **세션 범위**라 과대추정 가능 | `OBSERVED` (한계) · DV-02 |
| **한계 4** | GA4보다 적게 집계한다 — FIRST 기준 80.7% 수준 | `OBSERVED` |

### 1-4. 상태 체계

원자료에서 쓰인 용어를 우선한다.

| 상태 | 의미 |
|---|---|
| `CONFIRMED` | 결정 문서 또는 실측으로 확정된 것 |
| `OBSERVED` | 관측된 수치·사실 (해석이 아님) |
| `PROPOSED` | 제안된 것 — 확정 사양이 아님 (ME-01~04) |
| `UNVERIFIED` | 확인되지 않음 |
| `OPEN` | 결정·확인 대기 |
| `PROTOTYPE-ONLY` | 프로토타입 검증 장치 |
| `OUT OF SCOPE` | 본 프로젝트 범위 밖 |

원자료의 근거 등급 표기(`FACT` / `INTERPRETATION` / `STRONG` / `MODERATE` / `CONDITIONAL`)도 인용 시 그대로 유지한다.

---

## 02. 현행 태깅 실태 (PHASE 2 관측)

### 2-1. 관측된 사실

| 대상 | 관측 결과 | 상태 |
|---|---|---|
| `#phoneArea` 내부 링크·버튼 | **13개** | `OBSERVED` |
| 그중 GTM 태깅된 것 | **0개** | `OBSERVED` |
| 미태깅 요소 (명시 관측) | `이벤트 전체보기` · `더보기` · **휴대폰 상품 카드 전체** | `OBSERVED` |
| `MAIN_NOTLOGIN` 범위 **밖**인 요소 | `전체메뉴`(→`GNB`) · `로그인 배너`(→`UTIL_TOP`) · `푸터 영역 보기` · `검색`(→`UTIL_TOP`) | `OBSERVED` |
| 휴대폰 영역 GTM action | **없음** (요소 수 0) | `OBSERVED` |

### 2-2. "태깅이 없다" vs "범위가 아니다" — 구분

| 구분 | 해당 항목 | 의미 |
|---|---|---|
| **태깅이 없다** | `#phoneArea` 13개 · 이벤트 전체보기 · 더보기 · 휴대폰 카드 | GTM에 태깅 자체가 존재하지 않는다 |
| **다른 카테고리에 있다** | 전체메뉴 · 로그인 배너 · 푸터 영역 보기 · 검색 | 태깅은 있으나 `MAIN_NOTLOGIN` 집계에 포함되지 않는다 |
| **관측되지 않았다** | 위에 열거되지 않은 요소 | **태깅되지 않았다고 단정하지 않는다** `UNVERIFIED` |

> PHASE 2가 확인한 것은 위 목록까지다. 목록에 없는 요소의 태깅 유무는 관측 대상이 아니었다.

### 2-3. 판정에 미친 영향 (기록)

`#phoneArea` 미태깅으로 R-12(휴대폰) 판정이 `Not Validated` → **`Unknown`(측정 불가)** 로 변경됐다. "수요가 없어서가 아니라 태깅이 없어서"다. 다만 GA4 다음 페이지 이동은 태깅과 무관하게 집계되므로 **휴대폰 페이지 이동이 상위에 없다는 사실 자체는 유효**하다. `CONFIRMED`

---

## 03. 분모 정의

### 3-1. GA4 두 분모

| 분모 | FIRST | REVISIT | 출처 리포트 | 쓰이는 지표 |
|---|---:|---:|---|---|
| **세션** | 126,749 | 31,861 | GA4 참여도/이탈률 | 세션당 발생률 · 유입 경로 비율 |
| **경로탐색 시작 세션** | 130,607 | 33,402 | GA4 경로탐색 | "다음 행동" 비율 |

- 같은 기간(2026-07-28~08-24) · 같은 도구인데 리포트마다 세션 집계가 다르다. 원자료 기록 — **이탈률 126,749 / 경로탐색 130,607 / 채널 126,421**
- **차이의 GA4 내부 원인은 확인되지 않았다** `UNVERIFIED`
- **두 분모를 섞어 계산하지 않는다.** 각 수치에 어느 분모인지 표기한다 `CONFIRMED`(규칙)

### 3-2. FIRST / REVISIT 정의

| 구분 | 정의 |
|---|---|
| FIRST | 첫 방문으로 분류된 세션 — `/nologfirst` |
| REVISIT | 재방문으로 분류된 세션 — `/nologrevisit` |

> **이 분류는 쿠키로 판정한 방문 이력이며 사람의 가입 여부·신규 고객 여부가 아니다.** `CONFIRMED`
> 판정 자체가 도구마다 다르다 — 서비스 100% / 0% · Clarity 96.5% / 36.0% · GA4 77.6% / 26.9%. 분기 판정이 어긋난 세션 23.9%(39,957 중 9,541) `OBSERVED`
> 화면 차등은 만들지 않는다(PHASE 6). 측정에서는 **두 URL을 분리해 집계**한다.

### 3-3. Clarity 모수 3종 — 불일치 그대로 기록

| 값 (F / R) | 출처 | 기간 | 상태 |
|---|---|---|---|
| **105,438 / 29,209** | PHASE 5.1 원천 데이터 **S1** "페이지별 세션수" | 2026-07-28 ~ 08-24 | `OBSERVED` |
| **105,164 / 29,290** | `targetuser.html` — Clarity 자체 쿠키 판정의 분모 (101,514/105,164 · 10,562/29,290) | 기간 표기 없음 | `OBSERVED` |
| **104,367 / 29,198** | `index.html` 범례 · `alignment.html` 모수표 "Clarity 세션" | 기간 표기 없음 | **`UNVERIFIED` — 원자료 근거 미확인** |

> **원인을 추정하지 않는다.** 세 값 모두 원문 그대로 유지한다.
> `alignment.html` Appendix A가 104,367 / 29,198 에 붙인 "(Clarity 분모)" 라벨은 **어떤 지표의 분모인지 확인되지 않았다** `UNVERIFIED`.
> **표기 정정은 본 명세의 범위가 아니다** — 기존 문서를 수정하지 않는다.

### 3-4. 도구 간 모수 대조 (기록만)

| 대조 | 값 | 출처 |
|---|---:|---|
| Clarity FIRST 105,438 ÷ GA4 경로탐색 FIRST 130,607 | 80.7% | PHASE 5.1 |
| Clarity REVISIT 29,209 ÷ GA4 경로탐색 REVISIT 33,402 | 87.4% | PHASE 5.1 |

> 이 대조는 **모수 차이를 보이기 위한 기록**이며, 이 비율로 다른 수치를 보정해 사용하지 않는다.

---

## 04. Prototype log ↔ 운영 이벤트 대조표

| Prototype log | 의미 | 운영 이벤트 존재 여부 | 운영 이벤트 ID | 상태 | 비고 |
|---|---|---|---|---|---|
| `INIT` | 프로토타입 시작 | 해당 없음 | — | `PROTOTYPE-ONLY` | 화면 초기화 로그 |
| `RANGE_SELECT` | M-03 구간 선택 | **미확인** | — | `PROTOTYPE-ONLY` | 현행에 M-03 구조 자체가 없다(SearchFee는 다른 구간 체계) |
| `HANDOFF` | 조건 전달 시도 | **미확인** | — | `PROTOTYPE-ONLY` | 전달 자체가 선적용되지 않는 상태(TR-01) |
| `NAVIGATE` | 목적지 이동 | **미확인** | — | `PROTOTYPE-ONLY` | GA4 페이지 이동은 별개 집계 |
| `BACK` | 메인 복귀 | **미확인** | — | `PROTOTYPE-ONLY` | — |
| `M03_STATE` | M-03 상태 전환(검증 토글) | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 조작 |
| `M03_RETRY` | 조회 실패 재시도 | **미확인** | — | `PROTOTYPE-ONLY` | 상태 자체가 `PARTIAL`(CH-21) |
| `M04_STATE` | M-04 상태 전환(검증 토글) | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 조작 |
| `M05_STATE` | M-05 상태 전환(검증 토글) | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 조작 |
| `PHONE_TOGGLE` | M-06 펼침/접힘 | **미확인** | — | `PROTOTYPE-ONLY` | DV-01 — 현행 `#phoneArea` 태깅 0개 |
| `PHONE_TAB` | M-06 탭 전환 | **미확인** | — | `PROTOTYPE-ONLY` | DV-01 필요 측정 4종 중 "탭 전환"에 해당 |
| `ACC_TOGGLE` | M-07/M-08 접힘 토글 | **미확인** | — | `PROTOTYPE-ONLY` | — |
| `CAROUSEL` | 배너 자동 전환 재생/정지 | **미확인** | — | `PROTOTYPE-ONLY` | 현행 `ProductBanner`는 클릭 집계이며 정지 조작이 아니다 |
| `SEARCH_OPEN` | 검색 레이어 진입 | **미확인** | — | `PROTOTYPE-ONLY` | 검색은 `UTIL_TOP` 범위 · 실행 미구현 |
| `ATF_GUIDE` | AtF 가이드선 토글 | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 |
| `ANNOTATE` | 모듈 주석 토글 | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 |
| `AUDIT_OK` | 가격 감사 통과 | 해당 없음 | — | `PROTOTYPE-ONLY` | **SAMPLE 목록과의 내부 대조** |
| `AUDIT_FAIL` | 가격 감사 실패 | 해당 없음 | — | `PROTOTYPE-ONLY` | 동일 |
| `BLOCKED` | 비활성 요소 클릭 차단 | **미확인** | — | `PROTOTYPE-ONLY` | — |
| `RESET` | 프로토타입 초기화 | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 |
| `RESIZE` | 폭 전환 | 해당 없음 | — | `PROTOTYPE-ONLY` | 검증 도구 |

**21종 전부 `PROTOTYPE-ONLY`.** 운영 이벤트와 의미가 비슷해 보여도 자동 매핑하지 않았다.

---

## 05. 측정 이벤트 — ME-01 ~ ME-04

> **네 항목 모두 `PROPOSED` 이며 확정된 이벤트 사양이 아니다.** 원자료 표현 — "아래는 **제안이며 확정된 이벤트 사양이 아니다**."
> 공통 배경 — 현재 `MAIN_NOTLOGIN` 태깅에는 **진입 맥락을 구분하는 차원이 없어**, 복귀 세션만 분리해 행동을 보는 것이 불가능하다.

### ME-01 · 진입 맥락 차원

| 항목 | 내용 |
|---|---|
| 현재 상태 | **`PROPOSED`** · 미태깅 |
| 측정 대상 | 진입 맥락 — 일반 / 앱 웹뷰 / 로그인 오류 / 가입 완료 복귀 |
| event name | **원자료에 없음** `UNVERIFIED` |
| trigger | 원자료에 없음 `UNVERIFIED` |
| parameter | 원자료에 없음 `UNVERIFIED` |
| value | 원자료에 없음 `UNVERIFIED` |
| scope | 진입 1회 (RS-01 지속 정의 준거) |
| denominator | 미정 `OPEN` |
| FIRST / REVISIT | 분리 필요 (§3-2) · 방식 미정 |
| Before / After | **불가** — 현재 대응 이벤트 없음 |
| 현재 근거 | 리퍼러 관측 — `/util/join/joinComplete` 4,654 · `?userAgent=aos/ios` 3,494 · `/error?url=…api/login/after` 1,128 sessions `OBSERVED` |
| 미확정 | event name · parameter · 맥락 판정 수단 전부 |

> **ME-01은 RS-01 화면 변형의 선행 조건이다.** "이 측정이 붙기 전에는 복귀 맥락의 화면 변형을 확정하지 않는다." `CONFIRMED`(제약)

### ME-02 · 복귀 세션의 첫 상호작용

| 항목 | 내용 |
|---|---|
| 현재 상태 | **`PROPOSED`** · 미태깅 |
| 측정 대상 | 복귀 목적 — 로그인 / 요금제 탐색 / 개통 / 즉시 이탈 |
| event name · trigger · parameter · value | 원자료에 없음 `UNVERIFIED` |
| scope · denominator | 미정 `OPEN` |
| Before / After | **불가** |
| 현재 근거 | 복귀 목적은 **관측되지 않았다** `UNVERIFIED` |
| 의존 | ME-01 선행 |

### ME-03 · 복귀 → 로그인 성공

| 항목 | 내용 |
|---|---|
| 현재 상태 | **`PROPOSED`** · 미태깅 |
| 측정 대상 | 가입 직후 로그인이 실제로 이어지는지 · 로그인 오류 복귀 1,128 sessions와 겹치는지 |
| event name · trigger · parameter | 원자료에 없음 `UNVERIFIED` |
| denominator | 미정 `OPEN` |
| Before / After | **불가** |
| 현재 근거 | 리퍼러 관측 2종 `OBSERVED` |

### ME-04 · 전체메뉴 회원가입 클릭의 맥락별 분리

| 항목 | 내용 |
|---|---|
| 현재 상태 | **`PROPOSED`** · 미태깅 |
| 측정 대상 | 가입을 마치고 돌아온 세션에서도 회원가입을 다시 누르는지 |
| 대상 요소 | 전체메뉴 내 `/util/join/joinIntro` |
| **범위 유의** | 전체메뉴는 `GNB` 카테고리 — **`MAIN_NOTLOGIN` 범위 밖** `OBSERVED` |
| event name · parameter | 원자료에 없음 `UNVERIFIED` |
| Before / After | **불가** |
| 관련 결정 | CH-64 — 헤더 회원가입 링크는 철회됨. 억제가 필요한 상황이 실제로 있는지를 이 측정으로 먼저 확인한다 |

### 5-1. 측정 없이 확정하지 않는 것 (확정 제약)

> 원자료 원문 — "복귀 세션의 이탈률·다음 단계 전환율을 이 상태의 성과 지표로 쓰려면 **ME-01이 선행되어야 한다.** 지금 그 값을 KPI로 세우면 **측정할 수 없는 목표**가 된다." `CONFIRMED`

---

## 06. 기능 ↔ 측정 연결 (SPEC-02 F-01 ~ F-12)

**모든 기능에 이벤트가 있어야 한다고 가정하지 않는다.** 현재 측정되지 않는 기능은 그 사실을 기록한다.

| F-ID | 기능 | 현행 운영 측정 | Prototype log | 판정 |
|---|---|---|---|---|
| F-01 | QuickMenu 이동 | **있음** — GTM action `QuickMenu` (4항목 기준) `OBSERVED` | — | 개편 후 3항목으로 바뀌므로 **정의 변경**. DV-05 동일 단위 비교 대상 |
| F-02 | M-03 구간 선택 | **없음** — 현행 `SearchFee`는 **다른 구간 체계**(7GB~/15GB~/71GB~/100GB~/초저가) `OBSERVED` | `RANGE_SELECT` `PROTOTYPE-ONLY` | **신규 측정 필요** · 사양 `UNVERIFIED` |
| F-03 | 조건 전달 | **없음** | `HANDOFF` `PROTOTYPE-ONLY` | 전달이 선적용되지 않는 상태(TR-01)라 **측정 대상 자체가 미확정** `OPEN` |
| F-04 | 목록 진입 | **있음(간접)** — GA4 페이지 이동으로 `pricList` 도달 집계 가능 `OBSERVED` | `NAVIGATE` | 진입 경로별 분해는 `UNVERIFIED` |
| F-05 | 뒤로가기 / 재진입 | **없음** | `BACK` `PROTOTYPE-ONLY` | Quick back은 **Clarity 관측**이며 GA4 이벤트가 아니다 |
| F-06 | M-06 펼침 / 접힘 | **없음** — `#phoneArea` 태깅 0개 `OBSERVED` | `PHONE_TOGGLE` | **DV-01 필요 측정 4종** 중 "영역 노출(impression)"·"전체보기"와 연결 |
| F-07 | M-06 탭 전환 | **없음** | `PHONE_TAB` | DV-01 4종 중 "탭 전환(3탭)"·"상품 카드 클릭" |
| F-08 | M-04 캐러셀 | **부분** — `ProductBanner`(클릭) · `ProductProBanner`(기획전 배너) `OBSERVED` | `CAROUSEL` | **정지/재생·스와이프 조작은 미측정** `UNVERIFIED` |
| F-09 | M-05 개통 상태 | **부분** — `openingGuide` 도달 4,724 / 1,745 sessions `OBSERVED` | `M05_STATE` | **개통 불가 상태의 노출·차단은 미측정** `UNVERIFIED` |
| F-10 | GNB 열기 / 닫기 | **있음(다른 카테고리)** — `GNB` `OBSERVED` | — | `MAIN_NOTLOGIN` 범위 밖 · 내부 구조는 `OUT OF SCOPE` |
| F-11 | 검색 | **있음(다른 카테고리)** — `UTIL_TOP` · 클릭 4,765 / 1,185 `OBSERVED` | `SEARCH_OPEN` | **집계 필터가 세션 범위라 과대추정 가능**(DV-02) · 실행 미구현 |
| F-12 | 팝업 | **있음** — `PopupTop` · `PopupClose` · `Popup_79` · `Popup_95` `OBSERVED` | — | **닫기 3종 유형별 분해 불가** `UNVERIFIED` |

### 6-1. 현행 관측 수치 (원자료 그대로 · 분모 표기 포함)

| 지표 | 값 (F / R) | 분모 | 도구 |
|---|---|---|---|
| 다음 행동 — 로그인 | 43.9% / 21.6% (57,311 ÷ 130,607 · 7,207 ÷ 33,402) | 경로탐색 시작 세션 | GA4 |
| 다음 행동 — 요금제 | 13.3% / 17.2% (17,409 / 5,752) | 경로탐색 시작 세션 | GA4 |
| 다음 행동 — 이벤트 | 8.4% / 15.8% (10,933 / 5,280) | 경로탐색 시작 세션 | GA4 |
| 다음 행동 — 가입·개통 | 7.8% / 12.3% (10,156 / 4,099) | 경로탐색 시작 세션 | GA4 |
| 팝업 닫기 | 0.766 / 0.714 회/세션 (97,067 / 22,745) | 세션 | GA4 |
| QuickMenu | 0.101 / 0.106 회/세션 (12,745 / 3,385) | 세션 | GA4 |
| `SearchFee` 클릭 | 0.0079 / 0.0067 회/세션 | 세션 | GA4 |
| `CustomerCenter` | 0.0101 / 0.0102 회/세션 | 세션 | GA4 |
| `ProductBanner` / `ProductProBanner` | 0.0437 / 0.0758 · 0.0303 / 0.0294 회/세션 | 세션 | GA4 |
| 스크롤 도달 | 44.5% / 39.2% | Clarity 모수 | Clarity |
| Quick back | 54.8% / 39.3% | Clarity 모수 | Clarity |
| Dead / Rage click | 2.4% / 1.5% · 0.1% / 0.0% | Clarity 모수 | Clarity |
| 퀵메뉴 클릭 텍스트 세션 | 3,541 / 3,064 / 1,572 / 774 (F) | **분모 불요 — 순위 비교 전용** | Clarity |

> 수치를 재계산하거나 도구를 넘어 결합하지 않았다.

---

## 07. DV / OI 연결

### DV-01 · `#phoneArea` 측정 태깅

| 구분 | 사실 | 상태 |
|---|---|---|
| 실제 컨테이너 | `#phoneArea` **PHASE 2에서 직접 관측됨** | `OBSERVED` |
| 내부 링크·버튼 | 13개 | `OBSERVED` |
| GTM 태깅 | **0개** | `OBSERVED` |
| **내부 개별 요소 식별자** | **미관측 — 별도 확인 필요** | `UNVERIFIED` |
| 필요 측정 4종 (PHASE 6) | 영역 노출(impression) · 탭 전환(3탭) · 상품 카드 클릭 · 전체보기 클릭 | `PROPOSED` |
| 제약 | **태깅이 붙기 전까지 영역을 확대하지 않는다** | `CONFIRMED` |
| 해소 방법 | 4종 태깅 추가 후 재판정 | `OPEN` |

> 노출(impression) 측정의 목적은 **"안 보여서"인지 "관심 없어서"인지를 구분**하는 것이다 — 현재 이 구분이 불가능하다. `CONFIRMED`(목적)

### DV-03 · 팝업 개수 · 노출 규칙 · 재노출 주기

| 항목 | 내용 | 상태 |
|---|---|---|
| 관측된 것 | PC 메인 `#commonPopup.main-modal-popup.on` · 인디케이터 1~6(다중 팝업 캐러셀) · `전체 닫기`·`오늘 하루 그만 보기` 별도 요소 · 모바일은 관측 시점 `#commonPopup` 미존재 · 노출 조건 쿠키 `popupBlockPage_exhbt` 확인 | `OBSERVED` |
| 확인되지 않은 것 | **개수 · 노출 규칙 · 재노출 주기** | `OPEN` |
| 해소 방법 | CMS 또는 운영 담당자 확인 — **GA4로는 불가** | `OPEN` |
| 측정 데이터의 한계 | 팝업 닫기 0.766 / 0.714 회/세션은 **발생률**이며 노출 규칙을 알려주지 않는다 | `CONFIRMED` |

> **현재 측정 데이터만으로 운영 규칙을 확정하지 않는다.**

### DV-04 · 분모 기간 정합 — 상태 표기 상이

| 출처 | 기록 | 상태 |
|---|---|---|
| PHASE 7-4 프로토타입 이월 블록 | "**해소** — TR-01 · DV-02 · DV-04 · OI-01" | 해소 표기 |
| PHASE 5.1 파생 비율 검증 | S1(2026-07-28~08-24) ÷ S2(2026-07-29~08-25) — **분자와 분모의 조회 기간이 1일 어긋난다**(28일 중 1일, 약 3.6%). "Clarity MCP 재연결 시 동일 기간으로 재조회하면 확정 가능" | ⚠ 조건부 재현 |

> **두 기록을 병기한다. 어느 쪽으로도 임의 통합하지 않는다** `UNVERIFIED — 확인 필요`.
> 다만 PHASE 5.1이 판정에 미치는 영향을 이미 정리했다 — 퀵메뉴 4항목의 결정은 **순위(동일 도구·동일 조회·동일 기간의 내부 비교, 분모 불요)** 에 근거하므로 기간 오차의 영향을 받지 않는다 `CONFIRMED`.
> 삭제된 파생 비율(기여율 계열 8~12번 · 합계 8,951 · "1/10 규모")은 **재사용 금지** `CONFIRMED`.

### DV-05 · M-02 / M-03 배치 순서

| 항목 | 내용 | 상태 |
|---|---|---|
| 현재 | **초기값이며 확정이 아니다** | `CONFIRMED`(제약) |
| 사유 | M-03의 성과 이력이 없다 — 현행 `SearchFee`는 y1802로 도달선 밖이었다 | `OBSERVED` |
| 검증 설계 | 개편 후 **M-02 각 항목과 M-03 각 구간의 클릭을 동일 단위(세션당 클릭)로 수집해 비교** | `CONFIRMED`(방법) |
| 포함 대상 | CH-35 라벨 변경("요금제 찾기" → "전체 요금제") 효과도 이 측정에 포함 | `CONFIRMED` |
| 해소 | 개편 후 A/B 또는 사후 비교 | `OPEN` |

> 초기값과 확정값을 혼동하지 않는다.

### DV-06 · 푸터 '영역 보기' 클릭 목적

| 항목 | 내용 | 상태 |
|---|---|---|
| 등재 | PHASE 6 G | — |
| 이후 추적 | **PHASE 6 이후 어느 문서에도 등장하지 않는다** | **추적 누락** |
| 처리 | **추적 복원 필요** — 새로 정의하지 않는다 | `UNVERIFIED` |
| 해소 방법 (원자료) | 세션 녹화 확인 | `OPEN` |

### DV-07 · Clarity 스크롤 지표 정의

| 항목 | 내용 | 상태 |
|---|---|---|
| 내용 | "최대 도달 위치 ÷ 페이지 높이" **가정**의 확인 | `UNVERIFIED` |
| 영향 | PHASE 6 원문 — "**도달선 근거가 조건부**" | `CONFIRMED` |
| 추적 | PHASE 7-1 · PHASE 7-3 이월까지 추적됨 · **PHASE 7-4 이월 블록에서 누락** | 추적 누락 |
| 처리 | 도달선(y1461~1658)을 **확정 지표로 승격하지 않는다** | `UNVERIFIED` |

> 스크롤 도달률 44.5% / 39.2%는 관측값이지만, 이를 y좌표로 환산한 도달선은 **지표 정의가 확정되지 않은 파생값**이다.

### OI-06 · 대표 가격 데이터 소스

| 항목 | 내용 | 상태 |
|---|---|---|
| 성격 | **데이터 소스 문제이며 측정 문제가 아니다** | `CONFIRMED`(구분) |
| 처리 | **가격 자체를 측정 이벤트로 만들지 않는다** | `CONFIRMED`(제약) |
| 상세 | → SPEC-04 §04 · DQ-7 · DQ-8 | `UNVERIFIED` |

### OI-07 · sticky 헤더 부작용

| 항목 | 내용 | 상태 |
|---|---|---|
| 내용 | 헤더 고정 시 세로 공간 56px 상시 점유 · 실효 뷰포트가 700px 가정이므로 실측 후 재검토 | `UNVERIFIED` |
| 측정 필요성 | 실효 뷰포트 실측(OI-10)과 연동 | `OPEN` |
| 처리 | **새로운 KPI를 만들지 않는다** — 필요성과 기존 근거만 기록 | `CONFIRMED`(제약) |

### 기타 측정 관련 항목

| ID | 내용 | 상태 |
|---|---|---|
| DV-02 | 검색 우선순위 등급 — 검증 **종결**, 결론은 **승격 없이 유보 유지**. 근거: 클릭 4,765/1,185는 요금제찾기와 유사 규모이나 **집계 필터가 세션 범위라 과대추정 가능** | `CONFIRMED`(결론) |
| — | "검색 = 퀵메뉴급" · "전체메뉴가 퀵메뉴의 5배" · 교차모수 기여율 4건 · "1/10 규모" · 합계 8,951 | **철회 — 재사용 금지** `CONFIRMED` |
| — | 계정 사용 흔적 유입 9,381 / 3,405 sessions (7.4% / 10.7%, 분모=세션) | `OBSERVED` |
| — | `/my/main` 도달 44,514 / 118,537 = **37.6%** (2026-07-30~08-26) — 계정 보유자의 **하한선**이며 상한이 아니다 | `OBSERVED` |

---

## 08. 개선 전 / 후 비교

### 8-1. 비교 가능 — 동일 정의·동일 분모로 측정 가능한 것

| 지표 | 분모 | 도구 | 주의 |
|---|---|---|---|
| 다음 행동 비율 (로그인 / 요금제 / 이벤트 / 가입·개통) | 경로탐색 시작 세션 | GA4 | 개편 후에도 같은 리포트를 쓸 것 |
| 팝업 닫기 세션당 발생률 | 세션 | GA4 | 팝업 정책 미변경 시에 한함(D-07(FR-07)) |
| `CustomerCenter` 세션당 발생률 | 세션 | GA4 | 영역 변경 없음 |
| `pricList` 도달 | GA4 페이지 이동 | GA4 | 진입 경로별 분해는 불가 |
| 스크롤 도달률 | Clarity 모수 | Clarity | **DV-07 정의 미확정** — 방향성 비교로만 |
| Quick back · Dead / Rage click | Clarity 모수 | Clarity | 동일 |

### 8-2. 비교 불가 — 사유별

| 지표 | 사유 | 판정 |
|---|---|---|
| QuickMenu 세션당 클릭 | **정의 변경** — 4항목 → 3항목, 라벨 변경(CH-35) | **주의 필요** — 항목 단위로만 비교 가능 |
| M-03 구간 선택 | **기존 이벤트 없음** — 현행 `SearchFee`는 다른 구간 체계 | **비교 불가** |
| 조건 전달 | **이벤트 없음** · 기능 자체가 미적용(TR-01) | **비교 불가** |
| M-06 영역 성과 | **태깅 0개**(DV-01) — 개편 전 값이 존재하지 않는다 | **비교 불가** |
| 캐러셀 조작(정지/스와이프) | 기존 이벤트 없음 | **비교 불가** |
| 개통 불가 상태 노출 | 기존 이벤트 없음 | **비교 불가** |
| 팝업 닫기 유형별 | 기존 분해 불가 | **비교 불가** |
| 검색 | **집계 필터가 세션 범위** · `UTIL_TOP` 카테고리 | **주의 필요** |
| RS-01 복귀 세션 행동 | **ME-01 미태깅** | **비교 불가** |
| Clarity ↔ GA4 결합 지표 | **도구 상이** | **금지** |
| `SearchFee` ↔ M-03 | 위치·구간 체계·구성 전부 다름 | **비교 불가** |

> **임의의 Before / After 비교식을 만들지 않는다.** 위 표는 무엇이 비교 가능한지의 기록이며, 새 지표를 정의한 것이 아니다.

### 8-3. DD-05 기대 효과에 대한 조건 (승계)

TR-01이 불가로 확정될 경우 — "**DD-05의 기대 효과를 축소해 기술해야 한다**" (PHASE 7-1). 개선 효과 서술 시 이 조건을 적용한다. `CONFIRMED`

---

## 09. 측정 범위와 OUT OF SCOPE

| 영역 | 구분 | 근거 |
|---|---|---|
| 메인 본문 M-01~M-09 | **측정 범위** | 본 프로젝트 대상 화면 |
| **GNB** | **`OUT OF SCOPE`** — 명세하지 않는다 | PHASE 5.2 "주요 메뉴(GNB) Keep. 변경 없음" · `GNB` 카테고리는 `MAIN_NOTLOGIN` 밖 |
| **Footer** | **측정 범위**(M-09) 이나 DV-06 미해소 | PHASE 6 M-09 |
| **Search** | **범위 안이나 등급 유보** — 핵심 행동 취급 금지 | D-09(5.2) · DV-02 · `UTIL_TOP` 카테고리 |
| **Popup** | **범위 안이나 규칙 공백** | D-07(FR-07) · DV-03 |
| **목적지 화면** | **`OUT OF SCOPE`** | DC-04 미결 · 화면 주체 미정 |
| **`pricList`** | **`OUT OF SCOPE`** | 목록 화면 설계는 본 프로젝트 범위 밖 |
| **외부 페이지** (`/benefit/*` · `/support/*` · `/app/*` 등) | **`OUT OF SCOPE`** | 진입점만 본 명세 대상 |
| **메인_PC** | **`OUT OF SCOPE`** | PC는 별도 화면 존재(GTM 카테고리 관측) |

> **"측정하지 않는다"와 "현재 프로젝트 범위 밖이라 명세하지 않는다"는 다르다.** 위 표의 `OUT OF SCOPE`는 후자다.

---

## 10. 데이터 품질 규칙

| Rule | 내용 |
|---|---|
| **Rule 1** | 동일 의미의 이벤트라도 **이름이 다르면 자동으로 같은 이벤트로 취급하지 않는다** |
| **Rule 2** | **Prototype log 이름을 GA4/GTM event name으로 승격하지 않는다** |
| **Rule 3** | **분모 정의가 없는 비율은 KPI로 확정하지 않는다** |
| **Rule 4** | **Clarity와 GA4의 모수를 임의로 결합하지 않는다** (예: `GA4 세션 ÷ Clarity 세션`) |
| **Rule 5** | **미확정 이벤트의 parameter를 추정하지 않는다** |
| **Rule 6** | **SAMPLE 데이터의 값을 운영 측정 기준값으로 사용하지 않는다** |

**추가 유의 (원자료 근거)**

- 세션당 발생률은 **"세션의 N%가 했다"가 아니다.** 한 세션에서 여러 번 발생하면 중복 집계된다 — 팝업 닫기 0.766은 "세션 100건당 약 77회"의 뜻이다 `CONFIRMED`
- 클릭 텍스트 세션 합계는 **중복이 제거되지 않는다.** 항목별 규모의 합으로만 사용한다 `CONFIRMED`
- GA4 리포트마다 세션 집계가 다르다 — 어느 리포트의 값인지 항상 표기한다 `CONFIRMED`

---

## 부록 A. TC — 측정 검증

| TC ID | 검증 대상 | 기대 | 판정 |
|---|---|---|---|
| TC-05-01 | Prototype log ↔ 운영 이벤트 구분 | 21종 전부 `PROTOTYPE-ONLY` 표기 · 운영 이벤트로 사용되지 않음 | 가능 |
| TC-05-02 | 운영 event name 확인 | GTM/GA4 실제 event name 목록 | **검증 불가 — 개발/분석 확인 필요** |
| TC-05-03 | parameter 확인 | 실제 parameter 사양 | **검증 불가 — 개발/분석 확인 필요** |
| TC-05-04 | trigger 확인 | 실제 발화 조건 | **검증 불가 — 개발/분석 확인 필요** |
| TC-05-05 | denominator 표기 | 모든 비율에 분모 명시 | 가능 |
| TC-05-06 | 두 GA4 분모 구분 | 126,749/31,861 과 130,607/33,402 를 섞지 않음 | 가능 |
| TC-05-07 | FIRST / REVISIT 분리 집계 | 두 URL 분리 집계 가능 | 가능 |
| TC-05-08 | Before / After 비교 가능 여부 | §08 분류와 일치 | 가능 |
| TC-05-09 | `#phoneArea` 태깅 존재 여부 | 현재 기대 = **0개** | 가능(현 상태 재현) |
| TC-05-10 | `#phoneArea` 내부 요소 식별 | 태깅 대상 4종 특정 | **검증 불가 — 개발 확인 필요** |
| TC-05-11 | 팝업 유형별 측정 가능 여부 | 닫기 3종 분해 | **검증 불가 — 현재 분해 불가** |
| TC-05-12 | Clarity 모수 정합성 | 3종 값이 원문대로 병기됨 · 임의 통합 없음 | 가능 |
| TC-05-13 | DV-04 기간 정합 | 동일 기간 재조회 | **검증 불가 — Clarity 재조회 필요** |
| TC-05-14 | ME-01~04 상태 | 전부 `PROPOSED` · 확정 사양 아님 | 가능 |
| TC-05-15 | 철회 근거 미사용 | 5건이 문서·측정에 0회 | 가능 |
| TC-05-16 | 교차 도구 파생 비율 부재 | Clarity 분자 ÷ GA4 분모 형태 0건 | 가능 |
| TC-05-17 | 검색 측정 정의 | 등급 유보 유지 · 핵심 행동 미취급 | 가능 |
| TC-05-18 | 조건 전달 측정 | **판정 기준 없음** | **검증 불가 — 기능 미적용** |
| TC-05-19 | DV-05 동일 단위 비교 설계 | 세션당 클릭 단위로 M-02·M-03 수집 | **검증 불가 — 개편 후 가능** |
| TC-05-20 | DV-06 · DV-07 추적 복원 | 이월 목록에 존재 | 가능 |

**총 20건 — 판정 가능 12 · 검증 불가 8.** 가상의 PASS 조건을 만들지 않았다.

---

## 미확정 이월

원자료를 재대조해 **실제 상태**로 기록한다. 기존 ID를 변경하지 않는다.

| ID | 항목 | 상태 | 해소 경로 |
|---|---|---|---|
| **DV-01** | `#phoneArea` 태깅 4종 · 내부 요소 식별자 | `UNVERIFIED` · 태깅 전 확대 금지 | 태깅 추가 후 재판정 |
| **DV-03** | 팝업 개수 · 노출 규칙 · 재노출 주기 | `OPEN` | CMS / 운영 담당자 (GA4 불가) |
| **DV-04** | 분모 기간 정합 | **`UNVERIFIED` — 표기 상이** (7-4는 해소 / 5.1은 1일 오차 잔존) | Clarity 동일 기간 재조회 |
| **DV-05** | M-02 / M-03 배치 순서 | `OPEN` · **초기값이며 확정 아님** | 개편 후 동일 단위 비교 |
| **DV-06** | 푸터 '영역 보기' 클릭 목적 | `UNVERIFIED` · **추적 복원 필요** | 세션 녹화 확인 |
| **DV-07** | Clarity 스크롤 지표 정의 | `UNVERIFIED` · 도달선 확정 금지 | 지표 정의 확인 |
| **OI-06** | 대표 가격 소스 | `UNVERIFIED` · **측정 문제 아님** | SPEC-04 DQ-7 · DQ-8 |
| **OI-07** | sticky 헤더 부작용 | `UNVERIFIED` · 새 KPI 금지 | 실측(OI-10 연동) |
| **ME-01** | 진입 맥락 차원 | **`PROPOSED`** · RS-01 선행 조건 | 태깅 설계 확정 |
| **ME-02** | 복귀 세션 첫 상호작용 | **`PROPOSED`** · ME-01 의존 | 동일 |
| **ME-03** | 복귀 → 로그인 성공 | **`PROPOSED`** | 동일 |
| **ME-04** | 전체메뉴 회원가입 클릭 맥락별 분리 | **`PROPOSED`** · `GNB` 범위 유의 | 동일 |
| — | **실제 운영 event name** | `UNVERIFIED` | 개발/분석 확인 |
| — | **이벤트 parameter 사양** | `UNVERIFIED` | 개발/분석 확인 |
| — | 팝업 닫기 3종 유형별 측정 | `UNVERIFIED` | 태깅 설계 |
| — | 검색 측정 (등급 유보 유지) | `OPEN` | DV-02 결론 유지 |
| — | 조건 전달 측정 | `OPEN` | TR-04 해소 후 |
| — | Before / After 비교 기준 | §08 분류까지 · **KPI 확정 없음** | 개편 범위 확정 후 |
| — | Clarity 모수 104,367 / 29,198 의 근거 | `UNVERIFIED` | 원자료 확인 |
| — | GA4 리포트별 세션 집계 차이의 내부 원인 | `UNVERIFIED` | GA4 확인 |

**재대조 결과 이월하지 않는 항목**

| ID | 사유 |
|---|---|
| **DV-02** | 검증 **종결** — 결론은 "승격 없이 유보 유지". 미확정이 아니라 **결론이 내려진 상태** |
| **TR-01 · TR-02** | 실측으로 확정 (SPEC-04 §03) — 측정 항목이 아님 |
| **OI-01** | 해소 표기 · 브랜드값은 SPEC-01 소관 |

**본 명세에서 새로 만든 것**

| 항목 | 성격 |
|---|---|
| `TC-05-01` ~ `TC-05-20` | 검증 항목 ID (신규) |

**새 event name · parameter · KPI · 전환율 정의는 만들지 않았다.** ME-01~ME-04를 확정 이벤트로 승격하지 않았고, 신규 ME ID도 만들지 않았다.

---

## SPEC-02 · 03 · 04와의 정합

| 항목 | SPEC-05 | 타 SPEC | 충돌 |
|---|---|---|---|
| Prototype log 21종 | `PROTOTYPE-ONLY` · 대조표 | SPEC-02 §13 동일 | 없음 |
| ME-01~04 | `PROPOSED` | SPEC-03 §4-2 "제안" 동일 | 없음 |
| GA4 두 분모 | 126,749/31,861 · 130,607/33,402 | SPEC_PREFLIGHT M-H 동일 | 없음 |
| Clarity 3종 모수 | 병기 · 미통합 | READINESS 동일 | 없음 |
| DV-01 | 컨테이너 관측됨 / 내부 미관측 | SPEC-02 F-06·F-07 · SPEC-04 DQ-12 동일 | 없음 |
| DV-03 | `OPEN` | SPEC-02 F-12 · SPEC-03 M-10 동일 | 없음 |
| 검색 등급 | 유보 유지 | SPEC-02 F-11 · SPEC-03 동일 | 없음 |
| 대표 가격 | 측정 대상 아님 | SPEC-04 §04 (데이터 소스) | 없음 — 관점 분리 |
| SAMPLE | 측정 기준값 금지(Rule 6) | SPEC-04 §05 동일 | 없음 |

---

기준 · PHASE 2 현행 화면구조·GTM 관측 · PHASE 3 행동분석·분모 정의 · PHASE 5.1 상세 IA·파생 비율 검증 · PHASE 5.2 확정 결정 · PHASE 6 Design Decision·G Data Validation · PHASE 7-1 · 7-2 · 7-3 · 7-4 Prototype · SPEC-02 · 03 · 04 · SPEC_PREFLIGHT · PHASE9_SPEC_READINESS
본 명세는 관측된 사실과 기존 제안만을 정리한다. 새 KPI·event name·parameter·전환율 정의를 만들지 않았고, 도구 간 모수를 결합하지 않았다.
