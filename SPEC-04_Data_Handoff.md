# SPEC-04 · Data / Handoff Specification

U+유모바일 비로그인 모바일 메인 개편 · Development Specification
작성 2026-08-27 · 기준 커밋 `beea5bb`

**상태 표기** — `CONFIRMED` / `PARTIAL` / `UNVERIFIED` / `OPEN` / `PROTOTYPE-ONLY` / `SAMPLE` / `OUT OF SCOPE`

> 이 문서는 PHASE 5.2 ~ 7-4에서 **이미 확정된 결정**과 **실측으로 확인된 사실**을 개발 명세 형태로 옮긴 것이다. 새로운 UX·파라미터·API·백엔드 방식을 정의하지 않는다. 원자료에 없는 것은 `UNVERIFIED` 또는 `NEEDS CONFIRMATION`으로 남긴다.

---

## 01. 범위와 경계

### 1-1. 데이터 3구분

명세 전체에서 아래 세 가지를 절대 섞지 않는다.

| 구분 | 정의 | 예 | 명세에서의 취급 |
|---|---|---|---|
| **실데이터** | 운영 서비스가 공급하는 값 | 요금제 목록, 이벤트 배너, 휴대폰 상품 | 계약 요구만 기술. 값은 기술하지 않는다 |
| **관측값 (FACT)** | 실제 화면·DOM·운영 파라미터에서 직접 확인한 값 | 요금제 123종 구간 분포, `dan2=0~4` 대응 | 근거로 사용 가능. 출처·관측 시점 병기 |
| **SAMPLE / PROTOTYPE 표시값** | 프로토타입이 구조 검증을 위해 넣은 값 | `PLANS` 6종 · `SLIDES` 3종 · `PHONES` 12종 | **개발 명세값으로 사용 금지** |

### 1-2. 본 명세의 적용 범위

| 항목 | 범위 |
|---|---|
| 대상 화면 | `/nologfirst` · `/nologrevisit` — **두 URL 공통 · 화면 차등 없음** (PHASE 5.2 "FIRST/REVISIT Keep" · PHASE 6 "화면 차등은 만들지 않는다 · GTM 시그니처 해시 완전 일치 −1113317340") · `CONFIRMED` |
| 운영 단위 병합 | D-01(FR-07) 결정 대기 · `OPEN` |
| 포함 | 메인 화면이 **내보내는** 조건(구간·정렬)과 **소비하는** 데이터(대표 가격·배너·상품) |
| 제외 | 목적지 화면의 내부 데이터 처리 |

### 1-3. `pricList` 소관 경계

| 사안 | 소관 | 상태 |
|---|---|---|
| 메인에서 목록으로 **조건을 보내는 행위** | **본 명세** | 03절 |
| 목록이 그 조건을 **받아 적용하는 로직** | 개발 확인 (`pricList` 구현) | `OPEN` · TR-04 |
| 목록의 **보조 필터 UI 순서** (통신망 → 소진 시 속도) | D-05(5.2) · DD-06 | `OUT OF SCOPE` — 목록 화면 소관 |
| 목록의 필터·정렬 UI 재설계 | — | `OUT OF SCOPE` |
| 프로토타입의 `panePric` | — | `PROTOTYPE-ONLY` — 조건 전달 검증용 스텁이며 목록 화면 설계가 아니다 |

> PHASE 7-1 D 원문 — "목록에서 조건 변경 시 **목록 페이지의 기존 필터·정렬 UI 사용**". 메인은 목록의 UI를 바꾸지 않는다.

---

## 02. M-03 구간 ↔ 운영 파라미터 매핑

### 2-1. 5구간 ↔ `dan2` 대응표

`CONFIRMED` — 2026-08-26 운영 `pricList` 실측

| 구간 ID (프로토타입) | 구간 라벨 | `dan2` | 상태 |
|---|---|:---:|---|
| `r1` | 3GB 이하 | `0` | `CONFIRMED` |
| `r2` | 4~6GB | `1` | `CONFIRMED` |
| `r3` | 7~14GB | `2` | `CONFIRMED` |
| `r4` | 15~70GB | `3` | `CONFIRMED` |
| `r5` | 71GB 이상 | `4` | `CONFIRMED` |

- 대응은 **1:1**이며 변환 규칙이 필요 없다. PHASE 5.1이 B(필터 기준)를 채택한 사유 중 하나가 이것이다 — "동일 체계이므로 메인에서 고른 구간이 목록 필터에 그대로 적용된다."
- 구간 ID `r1~r5`는 **프로토타입 내부 식별자**다. 실제 구현이 이 문자열을 써야 한다는 요구가 아니다. 계약상 의미를 갖는 것은 `dan2` 값이다. `PROTOTYPE-ONLY`

### 2-2. `array` 파라미터

| 항목 | 내용 | 상태 |
|---|---|---|
| 관측된 파라미터 체계 | `dan1` ~ `dan7` · `array` | `CONFIRMED` |
| 실측에 사용된 값 | `array=discntaddvat` | `CONFIRMED` |
| `array`의 의미·역할 | **어느 원자료에도 정의가 없다** | **`UNVERIFIED` · NEEDS CONFIRMATION** |
| `dan1` · `dan3` ~ `dan7`의 의미 | 미확인 — `dan2`만 대응이 확인됐다 | `UNVERIFIED` |
| 정렬(가격 낮은 순) 전달 수단 | **미확인** — 정렬을 지정하는 파라미터가 무엇인지 관측되지 않았다 | **`UNVERIFIED` · NEEDS CONFIRMATION** |

> **추정 금지 항목** — `discntaddvat`라는 문자열의 의미를 해석해 정렬축·할인 조건 등으로 단정하지 않는다. 실측은 "이 값이 사용된다"까지만 확인했다.

### 2-3. 구간 경계값 근거

| 근거 | 내용 | 출처 | 상태 |
|---|---|---|---|
| 확정 결정 | `3GB 이하 / 4~6GB / 7~14GB / 15~70GB / 71GB 이상` | PHASE 5.1 결정 1 · PHASE 5.2 D-04 | `CONFIRMED` |
| 상품 분포 | 28 / 20 / 30 / 22 / 23 = 123종 · 비중 16.3~24.4% | DOM 직접 집계 | `CONFIRMED` (관측 시점 스냅샷) |
| 필터 사용 세션 | 2,210 / 1,810 / 3,465 / 3,963 / 3,036 | 운영 필터 데이터 | `CONFIRMED` |
| 동시 노출 요구 | 5행이 어떤 폭에서도 모두 보인다 · 가로 스크롤 부적합 | DD-04 · PHASE 7-1 C-03 | `CONFIRMED` |
| 별도 '초저가' 구간 | 두지 않는다 — 가격 병기로 대체 | PHASE 5.1 (IA-P3) | `CONFIRMED` |

**프로토타입 표시값과 운영 데이터의 구분**

프로토타입 `RANGES`에는 `count`(구간별 상품 수)와 `desc`(용도 설명)가 함께 들어 있다.

| 필드 | 성격 | 상태 |
|---|---|---|
| `label` · `dan2` | 확정 결정 + 실측 | `CONFIRMED` |
| `price` | 04절 참조 — 출처 표기가 문서 간 다름 | `PARTIAL` |
| `count` | DOM 관측 스냅샷 (28/20/30/22/23) | `CONFIRMED` (스냅샷) — **화면에 노출되지 않는다.** DD-07에 따라 결과 개수 표시는 두지 않는다 |
| `desc` | 운영 필터 라벨 승계 · 카피 미확정 | `OPEN` · OI-05 |
| `icon` · `tile` | 시각 표현 | SPEC-01 소관 |

---

## 03. 조건 전달 현황

### 3-0. 확정된 UX (변경 없음)

PHASE 7-1 D 원문 — "메인에서 구간을 고르면 **조건이 적용된 목록**에 도착한다 / 기본 정렬은 **가격 낮은 순** / 목록 화면에 **적용된 조건이 표시**된다 / 이 UX는 **구현 방식과 무관하게 유지**된다 — URL 파라미터든 상태 전달이든 결과는 같아야 한다."

`CONFIRMED` — DD-05. 이 절의 나머지는 **그 UX를 구현하는 수단의 현재 상태**를 기록한 것이며, UX 자체를 재론하지 않는다.

### 3-1. TR-01 — 조건 전달 가능 여부

| 항목 | 내용 | 상태 |
|---|---|---|
| 질문 (PHASE 7-1 원문) | "`pricList`가 진입 시점에 필터·정렬 초기값을 외부에서 받을 수 있는가" | — |
| 실측 일자 | 2026-08-26 | — |
| 실측 방법 | 운영 `pricList`에 `?dan2=2&array=discntaddvat` 로 직접 진입 | — |
| **실측 결과** | **선적용되지 않는다.** 전 항목 미체크 · 정렬 추천순 유지 | **`CONFIRMED`** |
| 판정 | TR-01 = **현재 상태에서 "선적용되지 않음"** | `CONFIRMED` |

> 이 결과는 "파라미터를 전달할 수 없다"가 아니라 "**전달해도 초기 상태에 반영되지 않는다**"는 뜻이다. `dan2` 초기값 전달과 실제 초기 필터 적용은 **별개의 문제**다(작성 원칙 7).

### 3-2. TR-02 — 전달 수단

| 항목 | 내용 | 상태 |
|---|---|---|
| 질문 (원문) | "수단은 무엇인가 — 쿼리스트링 / 해시 / 세션 상태 / 라우터 파라미터" | — |
| 확인된 수단 | **쿼리스트링** | `CONFIRMED` |
| 파서 | `_getParameterByName(name, url)` 가 **이미 존재하며 정상 파싱한다** | `CONFIRMED` |
| 파서의 위치·구현 내용 | 미확인 | `UNVERIFIED` |
| 파서 존재 = 초기화 적용 | **아니다.** 파싱 결과가 UI 초기 상태에 연결되어 있지 않다 | `CONFIRMED` (3-1 실측) |

### 3-3. TR-03A — 뒤로가기·새로고침 후 조건 유지

> **표기 안내** — 원 ID는 **TR-03** 하나다. 현재 문서들에서 이 ID가 **서로 다른 두 의미**로 쓰이고 있어, 본 명세 내부에서만 `TR-03A` / `TR-03B`로 구분해 표기한다. **신규 요구사항 ID를 만든 것이 아니며, 원 ID TR-03을 삭제하거나 변경하지 않는다.**

| 항목 | 내용 | 상태 |
|---|---|---|
| 출처 | PHASE 7-1 — "조건이 적용된 상태가 **뒤로가기·새로고침** 후에도 유지되는가" | — |
| 현재 확인 | 없음 | **`UNVERIFIED`** |
| 프로토타입 | 브라우저 히스토리 미구현 · 내부 pane 기준으로만 동작 | `PROTOTYPE-ONLY` |
| 처리 | 요구로 기록하되 구현 방식은 정하지 않는다 | 개발 확인 |

### 3-4. TR-03B — 목록 → 메인 복귀 시 선택 구간 유지

| 항목 | 내용 | 상태 |
|---|---|---|
| 출처 | `alignment.html` 15절 — "목록 → 메인 복귀 시 **선택 구간 유지** 여부 · **상세 설계 단계에서 정의(이번 범위 밖)**" | — |
| 현재 확인 | 없음 | **`UNVERIFIED`** |
| 범위 | 원자료가 명시적으로 **"이번 범위 밖"**으로 기록했다 | `OUT OF SCOPE`(현 단계) |
| 프로토타입 | 메인 복귀 시 스크롤 위치 · 선택 구간 · 접힘 상태를 복원하나, **메모리 전용**이며 실제 페이지 이동이 아니다 | `PROTOTYPE-ONLY` |

> TR-03A와 TR-03B는 **다른 질문이다.** A는 목록 화면 자체의 상태 지속, B는 메인 화면의 선택 상태 지속이다. 임의로 통합하지 않는다.

### 3-5. TR-04 — 초기화 로직 연결

| 항목 | 내용 | 상태 |
|---|---|---|
| 출처 (원문) | "미지원 시 대안 — 지원되는 축만 우선 전달, 또는 목록 진입 후 조건 자동 적용" | — |
| 실측이 좁힌 범위 | "파서는 이미 존재하고 정상 파싱하므로 **파싱 결과를 초기화 로직에 연결하는 작업만 남는다**" | `CONFIRMED` (사실 기록) |
| 연결 방식·영향 범위·작업량 | **미확인** | **`OPEN` · 개발 확인** |
| 본 명세의 태도 | 구현 방식을 정하지 않는다. 프론트 초기화·서버 렌더·라우터 등 어떤 방식도 명세에서 선택하지 않는다 | — |

**개발에 전달하는 확인 요청**

1. 파싱된 `dan2` 값을 목록 필터 초기 상태에 연결하는 것이 가능한가
2. 정렬 축(가격 낮은 순)을 초기값으로 지정할 수단이 있는가 — 2-2 참조, 수단 자체가 미확인
3. 연결 시 기존 필터 동작·북마크·SEO에 영향이 있는가

### 3-6. TR-01 불가로 확정될 경우의 대응 원칙

PHASE 7-1 원문을 그대로 승계한다. **새로운 대안 UX를 만들지 않는다.**

> "TR-01이 불가로 확인될 경우 **M-03의 구조는 바꾸지 않는다.** 구간 선택은 유지하고 조건 전달만 대안으로 처리한다. 최악의 경우에도 현행(조건 없이 목록 이동)보다 나쁘지 않으며, 구간별 진입 자체가 새로운 측정 축이 된다. **단, 이 경우 DD-05의 기대 효과를 축소해 기술해야 한다.**"

| 귀결 | 처리 |
|---|---|
| M-03 5구간 구조 | 변경 없음 · `CONFIRMED` |
| 조건 전달 | 대안 처리 · `OPEN` |
| DD-05 기대 효과 | **축소 기술 필요** — 개선 효과 서술 시 반영 (SPEC-05 연계) |
| 구간별 진입 측정 | 조건 전달 여부와 무관하게 성립 (SPEC-05 소관) |

---

## 04. 대표 가격 데이터

### 4-1. 현재 출처

| 항목 | 내용 | 상태 |
|---|---|---|
| 값 | 8,900 / 12,500 / 17,500 / 22,500 / 39,200 (원) | `CONFIRMED` (스냅샷) |
| 취득 방법 | 실서비스 요금제 목록 **DOM 직접 집계(123종)** | `CONFIRMED` |
| 성격 | **관측 시점의 스냅샷**이다. 실시간 값이 아니다 | `CONFIRMED` |
| 화면 표기 | "월 N,NNN원~" 형태 · 우측 정렬 · tabular 숫자 | PHASE 7-1 C-03 |

### 4-2. 값의 정의가 문서 간 다르다 — 병기

**임의로 통합하지 않는다.** 두 출처가 같은 숫자를 다른 이름으로 부른다.

| 출처 | 정의 | 상태 |
|---|---|---|
| PHASE 5.1 결정 1 표 | **중위 월요금** | `CONFIRMED` (원문) |
| PHASE 7-1 C-03 | "우측 대표 가격 'N원~' — 해당 구간 **최저가**" | `CONFIRMED` (원문) |
| 프로토타입 `auditPrices()` | 구간 **최저가**와 대조 (아래 주의 참조) | `PROTOTYPE-ONLY` |

> **NEEDS CONFIRMATION** — 표기 "N원~"은 최저가 의미에 부합하나, PHASE 5.1은 같은 값을 중위값으로 기술했다. **어느 정의가 운영 데이터 계약의 기준인지 확정되지 않았다.** 이 문서에서 어느 한쪽으로 정하지 않는다.

> **프로토타입 가격 감사에 대한 주의** — `auditPrices()`가 출력하는 "대표 가격 = 구간 최저가 5/5 일치"는 **프로토타입 내부 `PLANS`(SAMPLE) 목록과의 대조 결과**다. **운영 데이터와의 일치를 검증한 것이 아니다.** `PROTOTYPE-ONLY`

### 4-3. 실제 운영 소스 — 미확정 (OI-06)

| 항목 | 상태 |
|---|---|
| 데이터 소스 (API / CMS / 정적값 중 무엇인가) | **`UNVERIFIED`** |
| 갱신 주기 | **`UNVERIFIED`** |
| 요금 변경 시 반영 경로 | **`UNVERIFIED`** |
| 구간 최저가(또는 중위가) 산출 주체 (프론트 계산 / 서버 제공) | **`UNVERIFIED`** |

> 원자료에 기록된 것은 PHASE 7-2 OI-06 — "DOM 관측 스냅샷 기준의 표시값이다. **소스·갱신 주기 미정**"이 전부다. 그 이상을 추정하지 않는다. **API 구조·백엔드 구현 방식을 명세하지 않는다.**

### 4-4. 가격 미수신 시 데이터 계약 요구

확정된 **UI 처리 원칙**은 존재한다. 이를 데이터 계약 관점의 **요구**로만 기술한다.

| 확정 원칙 | 출처 | 상태 |
|---|---|---|
| 가격 칸만 비우고 **행과 이동은 유지**한다 | PHASE 7-1 상태 처리 원칙 | `CONFIRMED` |
| 스켈레톤·오류 문구를 넣지 않는다 | 동일 | `CONFIRMED` |
| 행을 감추거나 비활성화하지 않는다 | PHASE 7-1 C-03 | `CONFIRMED` |
| 근거 | "가격은 보조 정보다. 부재가 주 행동(구간 선택)을 막으면 안 된다" | `CONFIRMED` |
| M-03은 로딩 상태가 없다 — 구간 경계는 **정적 값** | PHASE 7-1 | `CONFIRMED` |

**데이터 계약에 대한 요구 (구현 방식은 지정하지 않음)**

1. 구간 5개의 **라벨·경계·순서는 가격 공급과 독립**해야 한다 — 가격이 없어도 5행이 렌더되고 이동이 가능해야 한다
2. 가격 항목은 **개별 구간 단위로 부재 가능**해야 한다 (전체 실패가 아닌 부분 부재)
3. 부재와 오류를 화면이 구분할 수 있어야 한다 — 부재는 04-4, 조회 실패는 SPEC-03 소관

> 프로토타입은 미수신 시 가격 칸에 `—`를 표시하고 행 클릭을 유지한다. 이는 **위 원칙의 한 가지 표현**이며, 실제 API 응답 형태·null 처리 규약을 정의한 것이 아니다. `PROTOTYPE-ONLY`

---

## 05. SAMPLE 데이터 경계

### 5-1. SAMPLE 목록

프로토타입 코드에 `[SAMPLE]` 주석으로 구분되어 있다.

| 변수 | 내용 | 건수 | 상태 |
|---|---|---:|---|
| `PLANS` | 요금제명 · 월정액 · 통신망 · 소진 시 속도 · 태그 | 6 | `SAMPLE` |
| `SLIDES` | 이벤트 배너 제목 · 부제 · 태그 | 3 | `SAMPLE` |
| `PHONES` | 휴대폰 상품명 · 가격 표기 (`zero` / `gal` / `iph` 각 4) | 12 | `SAMPLE` |

프로토타입 화면 하단 고지 — "표시된 요금제는 **구조 검증용 예시**입니다."

### 5-2. 사용 금지 규칙

1. SAMPLE 값(상품명·가격·태그·배너 문구)을 **개발 명세의 값으로 옮기지 않는다**
2. SAMPLE 건수(6 / 3 / 12)를 **노출 개수 요구로 확정하지 않는다** — 실제 개수는 운영 데이터에 따른다
3. SAMPLE 데이터 구조(필드 구성)를 **API 스키마로 제시하지 않는다** — 구조 검증을 위한 프로토타입 내부 형태다
4. `auditPrices()`의 "5/5 일치" 결과를 **운영 데이터 정합성 근거로 인용하지 않는다** (4-2 주의)

### 5-3. 실제 데이터 공급이 필요한 항목

| # | 항목 | 소비 모듈 | 현재 | 상태 |
|---|---|---|---|---|
| DS-1 | 구간별 대표 가격 | M-03 | DOM 스냅샷 | `UNVERIFIED` · OI-06 |
| DS-2 | 요금제 목록 | 목록 화면 | SAMPLE | `OUT OF SCOPE` (목록 소관) |
| DS-3 | 이벤트 배너 (개수 · 노출 조건 · 링크) | M-04 | SAMPLE | `UNVERIFIED` |
| DS-4 | 휴대폰 상품 (3탭 · 카드) | M-06 | SAMPLE | `UNVERIFIED` · DV-01 태깅 전 확대 금지 |
| DS-5 | 셀프개통 가능 시간대 | M-05 | 프로토타입 고정값 | `UNVERIFIED` |
| DS-6 | 팝업 콘텐츠 | M-10 | 없음 | `OPEN` · DV-03 |

> DS-3의 관련 확정 조건 — PHASE 6 M-04 "운영 등록 배너 존재 시 노출. **0건이면 영역 미노출**" `CONFIRMED`. 배너 개수 상한·정렬 규칙은 원자료에 없다 `UNVERIFIED`.
> DS-5의 관련 확정 조건 — PHASE 6 M-05 "시간대 안내는 **조건부 문구** — 개통 불가 시간에는 상태를 명시" `CONFIRMED`. 실제 시간대 값·판정 주체는 `UNVERIFIED`.

### 5-4. 실제 데이터 확보 전 허용되는 표현

| 허용 | 금지 |
|---|---|
| "구간별 대표 가격 1건을 공급받는다" | "`GET /api/plans/ranges` 로 받는다" |
| "배너가 0건이면 영역을 노출하지 않는다" (PHASE 6 확정) | "배너는 최대 5건까지 노출한다" |
| "가격이 없는 구간은 가격 칸을 비운다" (PHASE 7-1 확정) | "`price: null` 이면 빈 문자열을 렌더한다" |
| "상품 카드는 3탭 구성을 현행 유지한다" (PHASE 6 확정) | "탭당 4개씩 노출한다" (SAMPLE 건수) |

---

## 06. 개발 확인이 필요한 데이터 공급 항목

| # | 확인 항목 | 관련 ID | 상태 |
|---|---|---|---|
| DQ-1 | `array` 파라미터의 의미와 필수 여부 | TR-02 | `UNVERIFIED` |
| DQ-2 | `dan1` · `dan3`~`dan7` 의 의미 | TR-02 | `UNVERIFIED` |
| DQ-3 | 정렬(가격 낮은 순) 초기값을 지정할 파라미터의 존재 여부 | DD-05 · TR-02 | `UNVERIFIED` |
| DQ-4 | 파싱 결과를 초기 필터 상태에 연결하는 작업의 가능 여부·범위 | TR-04 | `OPEN` |
| DQ-5 | 조건 적용 상태의 뒤로가기·새로고침 유지 | TR-03A | `UNVERIFIED` |
| DQ-6 | 목록→메인 복귀 시 선택 구간 유지 | TR-03B | `UNVERIFIED` · 현 단계 범위 밖 |
| DQ-7 | 대표 가격의 소스·갱신 주기·반영 경로 | OI-06 | `UNVERIFIED` |
| DQ-8 | 대표 가격 정의 — 최저가 / 중위가 | OI-06 · 4-2 | `NEEDS CONFIRMATION` |
| DQ-9 | 가격 부분 부재를 표현할 수 있는지 (구간 단위) | — | `UNVERIFIED` |
| DQ-10 | 이벤트 배너 공급 방식과 0건 판정 주체 | DS-3 | `UNVERIFIED` |
| DQ-11 | 셀프개통 가능 시간 판정 주체 | DS-5 | `UNVERIFIED` |
| DQ-12 | `#phoneArea` 내부 요소 식별자 (태깅 대상 특정용) | DV-01 | `UNVERIFIED` — 컨테이너 id `#phoneArea`는 PHASE 2에서 관측됨 |
| DQ-13 | 상태 지속을 위한 저장소 사용 가능 여부·정책 | 07절 | `UNVERIFIED` |

---

## 07. 상태 지속 정책

### 7-1. 프로토타입의 현재 상태

| 항목 | 내용 | 상태 |
|---|---|---|
| 상태 보관 | 전역 객체 `S` — **메모리 전용** | `PROTOTYPE-ONLY` |
| `localStorage` / `sessionStorage` | **사용하지 않음** | `PROTOTYPE-ONLY` |
| 쿠키 | 사용하지 않음 | `PROTOTYPE-ONLY` |
| 보관 대상 | 화면·선택 구간·정렬·모듈 상태·스크롤 위치·접힘 상태 등 | `PROTOTYPE-ONLY` |

프로토타입 고지 원문 — "**저장소 미사용 · 상태는 메모리 전용**".

### 7-2. 이것을 실서비스 정책으로 확정하지 않는다

프로토타입이 저장소를 쓰지 않는 것은 **검증 환경의 구현 선택**이며, 실서비스가 저장소를 쓰지 않아야 한다는 요구가 아니다. 동시에 **저장소를 쓴다는 결정도 없다.**

### 7-3. 관련 확정 사항 (범위 제한)

RS-01(가입 완료 후 복귀)에 대해서는 **지속 범위가 이미 확정**되어 있다.

> PHASE 11 / CH-62 원문 — "지속 — **해당 진입 1회. 세션 전체로 확장하지 않는다** — 확장하려면 저장소가 필요하고, **그 필요를 정당화할 관측이 없다**." `CONFIRMED`

따라서 **RS-01을 근거로 저장소를 도입하지 않는다.** 다른 목적의 저장소 도입은 별도 판단 사안이다.

### 7-4. 분리 기록

| 사안 | 성격 | 상태 |
|---|---|---|
| 조건 적용 상태의 지속 (TR-03A) | 기술 확인 | `UNVERIFIED` · 개발 |
| 선택 구간의 지속 (TR-03B) | 기술 확인 | `UNVERIFIED` · 현 단계 범위 밖 |
| 저장소 사용 가능 여부 (기술 제약) | 기술 확인 | `UNVERIFIED` · 개발 |
| 저장소 사용 허용 여부 (개인정보·정책) | **서비스 정책 확인** | `UNVERIFIED` · 정책 |

> 위 네 가지는 서로 다른 확인 경로를 갖는다. 하나로 묶어 처리하지 않는다.

---

## 부록 A. TC — 데이터·전달 검증 항목

명세가 **검증 가능한 상태인지**를 확인하는 수준이며, 기능을 새로 설계하지 않는다.

| TC ID | 검증 대상 | 방법 | 기대 | 근거 | 현재 |
|---|---|---|---|---|---|
| TC-04-01 | 5구간 ↔ `dan2 0~4` 대응 | 각 구간 선택 시 생성되는 `dan2` 값 확인 | `r1→0 · r2→1 · r3→2 · r4→3 · r5→4` | DD-04 · §2-1 | 검증 가능 |
| TC-04-02 | 쿼리스트링 파싱 | `_getParameterByName`에 `dan2` 전달 후 반환값 확인 | 정상 파싱 | TR-02 · §3-2 | 검증 가능 |
| TC-04-03 | 초기 필터 선적용 | `?dan2=2&array=discntaddvat` 로 `pricList` 진입 | **현재 기대 = 선적용되지 않음** | TR-01 · §3-1 | 검증 가능 (현 상태 재현) |
| TC-04-04 | 초기 정렬 적용 | 동일 진입 후 정렬 상태 확인 | **현재 기대 = 추천순 유지** | TR-01 · §3-1 | 검증 가능 (현 상태 재현) |
| TC-04-05 | 가격 미수신 처리 | 가격 부재 상태에서 M-03 렌더 | 5행 유지 · 가격 칸만 비움 · 이동 가능 | §4-4 | 검증 가능 |
| TC-04-06 | 가격 부분 부재 | 일부 구간만 가격 부재 | 해당 행만 비고 나머지 정상 | §4-4 요구 2 | **DQ-9 확인 후** |
| TC-04-07 | 뒤로가기·새로고침 유지 | 조건 적용 후 새로고침 / 뒤로가기 | **판정 기준 미정** | TR-03A | **미확정 — 확인 후 작성** |
| TC-04-08 | 목록→메인 복귀 시 선택 유지 | 목록에서 메인 복귀 | **판정 기준 미정** | TR-03B | **미확정 · 범위 밖** |
| TC-04-09 | 배너 0건 처리 | 등록 배너 0건 | M-04 영역 미노출 | PHASE 6 M-04 | 검증 가능 |
| TC-04-10 | 대표 가격 정의 일치 | 표시값과 운영 목록 값 대조 | **정의 확정 후 판정** | DQ-8 | **미확정 — 확인 후 작성** |
| TC-04-11 | SAMPLE 미혼입 | 명세·구현에 SAMPLE 값 사용 여부 | SAMPLE 값 0건 | §05 | 검증 가능 |

> TC-04-03 · TC-04-04의 "기대"는 **현재 상태의 재현**이지 목표 상태가 아니다. TR-04 해소 후 기대값이 바뀌며, 그때 기대값을 갱신한다.

---

## 부록 B. 미확정 이월

기존 ID를 그대로 유지한다.

| ID | 항목 | 상태 | 해소 경로 |
|---|---|---|---|
| **TR-01** | 조건 전달 — 현재 선적용되지 않음 | `CONFIRMED`(사실) / 개선은 TR-04에 종속 | — |
| **TR-02** | 전달 수단 = 쿼리스트링 · 파서 존재 | `CONFIRMED` | — |
| **TR-03** (본 명세 표기 `TR-03A`) | 뒤로가기·새로고침 후 조건 유지 | `UNVERIFIED` | 개발 확인 |
| **TR-03** (본 명세 표기 `TR-03B`) | 목록→메인 복귀 시 선택 구간 유지 | `UNVERIFIED` | 상세 설계 단계 (원자료가 "이번 범위 밖"으로 명시) |
| **TR-04** | 초기화 로직 연결 | `OPEN` | 개발 확인 |
| **OI-06** | 대표 가격 소스·갱신 주기·반영 경로 | `UNVERIFIED` | 개발/운영 확인 |
| **DV-01** | `#phoneArea` 측정 태깅 | `UNVERIFIED` | 태깅 추가 후 재판정 · 태깅 전 영역 확대 금지 |
| **DV-03** | 팝업 개수·규칙·주기 | `OPEN` | 운영/CMS 확인 |
| **DC-04** | 목적지 화면 주체 | `OPEN` | 요청자 확정 |
| **D-01**(FR-07) | 운영 단위 병합 여부 | `OPEN` | 이해관계자 결정 |
| DQ-1 ~ DQ-13 | 06절 데이터 공급 확인 항목 | `UNVERIFIED` / `OPEN` | 개발·운영·정책 |

**본 명세에서 새로 만든 것**

| 항목 | 성격 |
|---|---|
| `TC-04-01` ~ `TC-04-11` | 검증 항목 ID (신규) |
| `DS-1` ~ `DS-6` · `DQ-1` ~ `DQ-13` | **문서 내부 목록 번호**이며 프로젝트 ID 체계에 편입하지 않는다 |
| `TR-03A` / `TR-03B` | **표기 구분**이며 신규 요구사항 ID가 아니다 |

새로운 UX 결정 ID · 정책 ID는 만들지 않았다.

---

## 추적성

| 근거 | SPEC-04 | 검증 |
|---|---|---|
| D-04(5.2) · DD-04 | §02 | TC-04-01 |
| DD-05 · TR-01 · TR-02 | §3-1 · §3-2 | TC-04-02 ~ TC-04-04 |
| TR-03(A) | §3-3 | TC-04-07 (미확정) |
| TR-03(B) | §3-4 | TC-04-08 (범위 밖) |
| TR-04 | §3-5 | DQ-4 |
| PHASE 7-1 상태 원칙 | §4-4 | TC-04-05 · TC-04-06 |
| OI-06 | §4-3 · §4-2 | TC-04-10 (미확정) |
| DD-07 | §2-3 (결과 개수 미노출) | SPEC-01 소관 |
| PHASE 6 M-04 노출 조건 | §5-3 | TC-04-09 |
| DD-14 | §05 (총량 불증가 — SAMPLE 확장 금지) | TC-04-11 |
| D-05(5.2) · DD-06 | §1-3 | `OUT OF SCOPE` |

---

기준 · PHASE 5.1 상세 IA · PHASE 5.2 확정 결정 · PHASE 6 화면 구조 · PHASE 7-1 UI 설계 · PHASE 7-4 Prototype(2026-08-26 실측 포함) · PHASE 8 GAP Analysis · SPEC Preflight · PHASE 2 현행 DOM 관측
미확정 이월 · TR-03A · TR-03B · TR-04 · OI-06 · DV-01 · DV-03 · DC-04 · DQ-1~13
본 명세는 확정된 결정과 실측 사실만을 기술한다. API 구조·백엔드 구현 방식·GA4 이벤트·UI 수치는 이 문서에서 정하지 않는다.
