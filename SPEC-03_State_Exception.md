# SPEC-03 · State & Exception Specification

U+유모바일 비로그인 모바일 메인 개편 · Development Specification
작성 2026-08-27 · 기준 커밋 `beea5bb` · 선행 `SPEC-04_Data_Handoff.md`

**상태 표기** — `CONFIRMED` / `PARTIAL` / `UNVERIFIED` / `OPEN` / `PROTOTYPE-ONLY` / `SAMPLE` / `OUT OF SCOPE` / `SPEC GAP`

> 이 문서는 프로토타입을 설명하지 않는다. PHASE 5.2 ~ 7-4에서 **확정되었거나 명시적으로 필요하다고 기록된** 상태·예외를 개발 가능한 수준으로 옮긴다. 새로운 오류 UX·빈 상태·팝업을 설계하지 않는다.

---

## 01. 상태 정의 원칙

PHASE 7-1 "Empty / Loading / Error — 모듈 상태 처리 원칙"을 승계한다. 아래 5개는 **원문 그대로의 확정 원칙**이며 본 명세가 새로 만든 것이 아니다.

| # | 원칙 | 근거 | 상태 |
|---|---|---|---|
| SP-1 | 정상 상태만을 기준으로 하지 않는다 | PHASE 7-1 | `CONFIRMED` |
| SP-2 | **부분 실패는 부분 처리한다** — "해당 섹션만 재시도. 화면 전체 오류로 처리하지 않는다" | PHASE 7-1 (M-06 펼침 실패) | `CONFIRMED` |
| SP-3 | **P0는 네트워크 실패로 사라지지 않는다** — "M-01·M-02·M-03은 항상 렌더된다. 오류는 실패한 영역에만 표시" | PHASE 7-1 (전역 네트워크 오류) | `CONFIRMED` |
| SP-4 | **빈 상태를 과잉 설계하지 않는다** — "일러스트·안내 문구가 있는 빈 상태 화면은 만들지 않는다. 콘텐츠가 없으면 **영역을 감추는 것이 기본**이다" | PHASE 7-1 | `CONFIRMED` |
| SP-5 | SP-4의 **유일한 예외** — "사용자가 부재 사실을 알아야 손해를 피하는 경우(M-05 개통 불가 시간)뿐이다" | PHASE 7-1 | `CONFIRMED` |

**본 명세가 하지 않는 것**

- 새로운 오류 화면·오류 문구 체계·재시도 정책을 설계하지 않는다
- 팝업 노출 규칙, 검색 실행 방식, WebView 처리 방식, 로그인 오류 처리 UX를 정하지 않는다
- 프로토타입에 구현된 상태라는 이유만으로 운영 요구사항으로 승격하지 않는다

---

## 02. 모듈별 상태

각 표의 **근거** 열이 비어 있지 않은 항목만 확정 요구다.

### M-01 · Header (P0)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `default` | 로고 · 검색(아이콘) · 로그인(텍스트+아이콘) · 전체메뉴(아이콘) | PHASE 7-1 C-01 | `CONFIRMED` |
| `sticky` | 스크롤 위치와 무관하게 고정. 앵커 이동 시 헤더 높이만큼 오프셋 보정 | PHASE 7-1 Navigation | `CONFIRMED` |
| `scrolled` | 스크롤 시 그림자 전환 — 고정 헤더와 콘텐츠 경계를 구분 | CH-20 (프로토타입 신설) | **`PARTIAL`** — 상위 Decision 없음. 시각 처리는 SPEC-01 소관 |

- 전환 임계값(프로토타입은 `scrollTop > 4`)은 **`PROTOTYPE-ONLY`**. 명세에서 수치를 확정하지 않는다.
- **하단 고정 바를 두지 않는다** — PHASE 7-1 원문: "비로그인 메인에는 전 화면 공통의 단일 주행동이 없다. 두 P0(로그인·요금제)가 병존하므로 하단 바가 한쪽을 임의로 승격시킨다" `CONFIRMED`
- sticky 헤더의 세로 공간 상시 점유 부작용은 **OI-07 미해소** `UNVERIFIED`

### M-02 · QuickMenu (P2)

| 상태 | 근거 | 상태 |
|---|---|---|
| `default` / `pressed` / `focus` | PHASE 7-1 C-02 | `CONFIRMED` |

- 3등분 그리드는 어떤 폭에서도 2행으로 접히지 않는다(라벨을 줄인다) — PHASE 7-1 E `CONFIRMED`
- 항목 추가·제거·순서 변경 없음 — D-01(5.2) · DD-02 `CONFIRMED`

### M-03 · 요금제 탐색 (P0)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `default` | 5행 **즉시 렌더** — "구간 경계는 정적 값이므로 **로딩이 없다**" | PHASE 7-1 | `CONFIRMED` |
| `selected` | 선택된 구간 1개 표시. **단일 선택** — 다중 선택·결과 개수 없음 | DD-07 · PHASE 7-1 D | `CONFIRMED` |
| `focus` / `pressed` | 행 전체가 탭 영역 | PHASE 7-1 C-03 | `CONFIRMED` |
| `가격 미수신` | **가격 칸만 비우고 행과 이동은 유지.** 스켈레톤·오류 문구를 넣지 않는다. 행을 감추거나 비활성화하지 않는다 | PHASE 7-1 상태 원칙 · C-03 | `CONFIRMED` |
| `조회 실패` | 전 구간 `aria-disabled` + 인라인 오류 + 재시도 | **CH-21 (프로토타입 신설)** | **`PARTIAL`** |
| `retry` | 재시도 후 재조회 | CH-21 | **`PARTIAL`** |

**`조회 실패` / `retry`에 대한 주의**

- PHASE 6 · PHASE 7-1 어디에도 이 상태의 정의가 없다. **프로토타입에서 신설**됐고 CH-21이 그 근거를 "7-1 '부분 실패는 부분 처리' 규칙"의 확장으로 기록했다.
- 따라서 **원칙(SP-2)은 확정, 이 상태의 구체 동작은 `NEEDS CONFIRMATION`**이다.
- 다만 SP-3(P0는 항상 렌더)과의 관계가 확인되지 않았다 — 전 구간을 `aria-disabled`로 두는 것이 "P0가 사라지지 않는다"와 어떻게 양립하는지 원자료에 판단이 없다. **`UNVERIFIED` — 확인 필요.**
- 오류 문구("요금제 정보를 불러오지 못했습니다") 는 **`PROTOTYPE-ONLY`**. 카피는 확정 대상이 아니다.

**M-03에서 금지되는 것 (확정)** — 추천·개인화 정렬, 메인 내 필터 적용·결과 갱신, 결과 개수·미리보기, 다중 선택, 특정 구간만 강조. DD-07 · PHASE 7-1 D · F `CONFIRMED`

### M-04 · 이벤트 · 프로모션 (P1)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `default` | 배너 캐러셀 + 전체보기 | PHASE 6 M-04 | `CONFIRMED` |
| `loading` | **배너 1장 높이의 자리를 미리 확보한 스켈레톤** (레이아웃 시프트 방지) | PHASE 7-1 | `CONFIRMED` |
| `등록 0건` | **영역 전체 미노출.** "진행 중인 이벤트가 없습니다" 빈 상태를 두지 않는다 | PHASE 7-1 · PHASE 6 F | `CONFIRMED` |
| `playing` / `paused` | 자동재생 + **정지 컨트롤** | PHASE 7-1 C-05 | `CONFIRMED` |
| `swipe` | 가로 슬라이드 | PHASE 7-1 C-05 | `CONFIRMED` |

- 배너에 테두리 강조·모션 강조를 더하지 않는다 — UI-P4 "운영 영역은 P0를 넘지 못한다" `CONFIRMED`
- `prefers-reduced-motion` 환경에서 자동재생을 시작하지 않는다 — PHASE 7-1 G 접근성 `CONFIRMED`
- 배너 **개수 상한·정렬 규칙**은 원자료에 없다 `UNVERIFIED` (SPEC-04 DS-3)
- 자동재생 주기·전환 시간은 **`PROTOTYPE-ONLY`**

### M-05 · 가입 · 개통 (P1)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `개통 가능` | 가입 안내 · 셀프개통 2타일 정상 + 가능 시간 인라인 표기 | PHASE 6 M-05 | `CONFIRMED` |
| `개통 불가` | **C-06을 `disabled`로 두고 C-07 인라인으로 사유·가능 시간 명시. 팝업 금지** | PHASE 7-1 · DD-09 · UI-P3 | `CONFIRMED` |

- 이 상태는 SP-4(빈 상태 감춤)의 **유일한 예외**다 — "사용자가 부재 사실을 알아야 손해를 피하는 경우" `CONFIRMED`
- 시간대 안내는 팝업이 아니라 **인라인 상시 표기**다 — 시안 R-09의 팝업안을 인라인으로 전환한 결정 DD-09 `CONFIRMED`
- **실제 가능 시간 값은 확정되지 않았다.** 프로토타입의 `09:00 ~ 20:00`은 **어느 결정 문서에도 없고 프로토타입 코드에만 존재한다** → `PROTOTYPE-ONLY`. 시간 값·판정 주체는 `UNVERIFIED` (SPEC-04 DS-5 · DQ-11)

### M-06 · 휴대폰 (NDV)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `collapsed` | **기본값**. 접힘 상태로 진입한다 | DD-11 · PHASE 7-1 C-08 | `CONFIRMED` |
| `loading` | **펼칠 때 콘텐츠를 로드한다** — 성과 미검증 영역에 초기 로딩 비용을 쓰지 않는다 | DD-11 | `CONFIRMED` |
| `expanded` | 3탭(인기 0원폰 / 반값 갤럭시 / 반값 아이폰) + 상품 카드 — **현행 유지** | PHASE 6 M-06 | `CONFIRMED` |
| `펼침 실패` | **해당 섹션만 재시도. 화면 전체 오류로 처리하지 않는다** | PHASE 7-1 상태 원칙 | **`SPEC GAP` — 프로토타입 미구현** |

- **영역 확대 금지** — DV-01 태깅이 붙기 전까지 확대하지 않는다. 강조·시각 비중을 부여하지 않는다 `CONFIRMED`
- M-02의 휴대폰 수요를 M-06의 근거로 인용하지 않는다 — D-03(5.2) `CONFIRMED`
- 탭 라벨 문구는 `UNVERIFIED` — 프로토타입 표기와 PHASE 6 표기가 다르다(§07 참조)

### M-07 / M-08 · 브랜드·혜택 / 고객지원

| 상태 | 근거 | 상태 |
|---|---|---|
| `collapsed` / `expanded` | PHASE 7-1 C-08 · PHASE 6 | `CONFIRMED` |

- M-07 운영 슬롯 규칙(브랜드 배너 고정 2건의 교체 주기 등)은 **DC-03 미결** `OPEN`
- 신규 영역을 추가하지 않는다 — DD-14 `CONFIRMED`

### M-09 · Footer

| 상태 | 근거 | 상태 |
|---|---|---|
| `default` / `focus` | PHASE 7-1 C-09 (LinkList — 카드 형태를 쓰지 않는다) | `CONFIRMED` |

- '푸터 영역 보기' 클릭 목적은 **DV-06 미해소** `UNVERIFIED`

### M-10 · 팝업

| 항목 | 내용 | 상태 |
|---|---|---|
| 구조 | 딤 + 콘텐츠 + **닫기 3종**(닫기 / 오늘 하루 그만 / 전체 닫기) | PHASE 7-1 C-10 | `CONFIRMED` |
| 노출 조건 · 개수 · 동시 노출 상한 · 재노출 주기 | **정의하지 않는다** | DD-12 · PHASE 7-1 · DV-03 | **`OPEN`** |
| 진입 시 자동 노출 | 규칙이 없으므로 **명세하지 않는다** | DV-03 | **`OPEN`** |

> PHASE 6 I 전제 승계 — "**규칙이 필요한 화면은 그릴 수 없다.** 구조 설계는 가능하나 규칙은 비워둔다." 이 절의 공백은 누락이 아니라 **의도된 공백**이다.
> 프로토타입은 팝업을 수동 호출로만 열며 진입 시 자동 노출이 없다. 이는 **검증 편의**이며 "자동 노출을 하지 않는다"는 운영 결정이 아니다. `PROTOTYPE-ONLY`

### M-11 · 전체메뉴 (GNB)

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `open` | 오버레이. **배경 스크롤 잠금** · 로그인 진입 유지 | PHASE 7-1 Navigation · C-11 | `CONFIRMED` |
| `closed` | 닫은 뒤 호출 지점으로 포커스 복귀 | PHASE 7-1 G | `CONFIRMED` |
| 내부 메뉴 트리 | — | **`OUT OF SCOPE`** |

**범위 경계 (명시)**

- PHASE 5.2 확정 — "**주요 메뉴(GNB) Keep. 변경 없음.** 퀵메뉴 축소가 GNB에 영향 주지 않음 — 이벤트·휴대폰은 GNB에 그대로 존재"
- 따라서 본 명세는 **진입점과 오버레이 동작만** 정의하고 내부 구조를 정의하지 않는다
- 프로토타입의 GNB 목적지 16개는 `PROTOTYPE-ONLY` — 실제 GNB 구조는 `UNVERIFIED`
- PHASE 1 OPEN-12 — "2026.01 GNB 개편안 존재 확인됨 — **관계 미정**" `OPEN`

### 검색

| 상태 | 정의 | 근거 | 상태 |
|---|---|---|---|
| `아이콘` | 헤더에 **아이콘으로** 둔다. 펼친 입력창을 두지 않는다 | D-09(5.2) · DD-13 · PHASE 7-1 | `CONFIRMED` |
| `open` | 레이어 진입 | PHASE 7-4 | `PARTIAL` |
| `입력` | 입력 가능 | PHASE 7-4 | `PARTIAL` |
| `최근 검색어 없음` | 빈 상태 표시 | PHASE 7-4 | **`PROTOTYPE-ONLY`** |
| `실행` | **미구현** | — | **`OPEN`** |

**검색 등급 — DV-02의 결론을 그대로 따른다**

| 항목 | 내용 |
|---|---|
| DV-02 검증 상태 | **종결**(2026-08-26) |
| 그 결론 | **승격 없이 유보 유지** — "아이콘 유지 · **핵심 행동 취급 금지**" |
| 판단 근거 | "검색 4,765/1,185 clicks는 요금제찾기와 유사 규모이나, **집계 필터가 세션 범위라 과대추정 가능**" |
| 철회된 근거 | "검색 = 퀵메뉴급"은 교차도구 비율이므로 **철회 상태 · 재사용 금지** |

> **검증 종결을 검색 기능 확정으로 해석하지 않는다.** 검색 실행 방식·결과 화면·우선순위는 본 명세에서 정하지 않는다. `OPEN`

---

## 03. 전역 상태

### 3-1. 전역 네트워크 오류

| 항목 | 내용 | 상태 |
|---|---|---|
| 확정 원칙 | **"M-01·M-02·M-03은 항상 렌더된다. 오류는 실패한 영역에만 표시"** | PHASE 7-1 · SP-3 `CONFIRMED` |
| 사유 | "P0가 네트워크 실패로 사라지지 않는다" | `CONFIRMED` |
| 프로토타입 구현 | **없음** — `네트워크` / `offline` / `navigator.onLine` 관련 코드 0건 | **`SPEC GAP`** |
| 오류 화면 디자인 | 본 명세에서 정하지 않는다 | `OPEN` |

**개발 요구로 남기는 것**

1. M-01 · M-02 · M-03은 다른 영역의 실패와 무관하게 렌더된다
2. 실패는 그 영역 안에서만 표현한다 — 화면 전체를 오류로 대체하지 않는다
3. M-03은 구간 경계가 정적 값이므로 네트워크 실패와 무관하게 5행이 존재해야 한다 (§02 M-03 `default`)

> 3번과 M-03 `조회 실패` 상태(CH-21)의 관계는 확인되지 않았다 — §02 M-03 주의 참조 `UNVERIFIED`

### 3-2. 로딩 및 빈 상태

| 규칙 | 근거 | 상태 |
|---|---|---|
| 콘텐츠가 없으면 **영역을 감춘다** (기본) | SP-4 | `CONFIRMED` |
| 일러스트·안내 문구가 있는 빈 상태 화면을 만들지 않는다 | SP-4 | `CONFIRMED` |
| 유일한 예외 = M-05 개통 불가 (인라인) | SP-5 | `CONFIRMED` |
| M-04 로딩은 **자리를 확보한 스켈레톤** | PHASE 7-1 | `CONFIRMED` |
| M-03은 로딩 상태가 없다 | PHASE 7-1 | `CONFIRMED` |
| M-06은 펼칠 때 로드하므로 로딩이 존재한다 | DD-11 | `CONFIRMED` |

**새로운 빈 상태 화면이나 팝업을 만들지 않는다.**

### 3-3. 오버레이 공통 (M-10 · M-11 · 검색)

| 항목 | 근거 | 상태 |
|---|---|---|
| 열림 시 배경 스크롤 잠금 | PHASE 7-1 Navigation | `CONFIRMED` |
| 포커스 트랩 · ESC 닫기 · 닫은 뒤 호출 지점 복귀 | PHASE 7-1 G | `CONFIRMED` |

### 3-4. 내비게이션 상태 (7-1 확정 · 수단 미확정)

| 요구 | 근거 | 상태 | 수단 |
|---|---|---|---|
| **메인 복귀 시 스크롤 위치 복원** | PHASE 7-1 Navigation | `CONFIRMED` (요구) | `UNVERIFIED` |
| **메인 복귀 시 접힘 상태(C-08) 유지** | PHASE 7-1 Navigation | `CONFIRMED` (요구) | `UNVERIFIED` |
| **같은 세션 재진입 시 접힘·닫힘 상태 유지** | PHASE 7-1 Navigation | `CONFIRMED` (요구) | `UNVERIFIED` |
| 근거 | "Quick back 54.8% / 39.3% — 되돌아오는 행동 자체가 흔하다" | `CONFIRMED` | — |

> **표기 안내** — 원 ID는 **`TR-03`** 하나다. 이 ID가 서로 다른 두 의미로 쓰이고 있어 본 명세 내부에서만 `TR-03A`(뒤로가기·새로고침 후 조건 유지) / `TR-03B`(목록→메인 복귀 시 선택 구간 유지)로 구분해 표기한다. **신규 요구사항 ID를 만든 것이 아니며, 원 ID `TR-03`을 삭제하거나 변경하지 않는다.**

> **SPEC-04와의 관계** — SPEC-04 §07은 저장소 사용 여부를 `UNVERIFIED`로 남겼다. 위 세 요구는 **무엇을 유지할지(요구)는 확정**이고 **어떻게 유지할지(수단)는 미확정**이다. 두 문서는 충돌하지 않으며, 수단은 SPEC-04 DQ-13 · TR-03A · TR-03B에서 확인한다.
> 프로토타입은 이를 메모리 전용 `S` 객체로 구현했다 — `PROTOTYPE-ONLY`. 실서비스 구현 방식이 아니다.

---

## 04. 진입 상태

### 4-1. FIRST / REVISIT

| 항목 | 내용 | 상태 |
|---|---|---|
| 화면 차등 | **만들지 않는다** | PHASE 6 STATE · PHASE 5.2 "FIRST/REVISIT Keep" `CONFIRMED` |
| 근거 | 두 페이지 GTM 시그니처 해시 완전 일치 `−1113317340` (DOM 단독) | `CONFIRMED` |
| 진입 맥락 차등 | 2곳 유지 (PHASE 5.2 원문) | `CONFIRMED` |
| 운영 단위 병합 여부 | D-01(FR-07) 결정 대기 | `OPEN` |

> **해석 제한 (확정)** — FIRST / REVISIT은 **쿠키로 판정한 방문 이력**이며 **사람의 가입 여부가 아니다.** 판정 자체가 도구마다 다르다(서비스 100%/0% · Clarity 96.5%/36.0% · GA4 77.6%/26.9%). 이 분류를 신규/기존 고객으로 환산하지 않는다.
> 분기 판정이 어긋난 세션 23.9%(39,957 중 9,541) — 판정 실패 시의 처리는 4-2의 원칙(안전한 쪽이 기본)과 별개 사안이며 본 명세에서 정하지 않는다 `OPEN`

### 4-2. RS-01 · 가입 완료 후 복귀

| 항목 | 정의 | 상태 |
|---|---|---|
| 상태 ID | `RS-01` — entry context `joined` | `CONFIRMED` |
| 진입 조건 | 직전 문서가 `/util/join/joinComplete` 인 상태로 비로그인 메인 도착 | `CONFIRMED` |
| 관측 규모 | 4,654 sessions (GA4 리퍼러) | `CONFIRMED` |
| 범위 | **화면 구조가 아니라 진입 시 처리.** M-01~M-11의 배치·순서·개수를 변경하지 않는다 | `CONFIRMED` |
| 지속 | **해당 진입 1회.** 세션 전체로 확장하지 않는다 | `CONFIRMED` |
| 기본값 | **일반 진입.** 판정에 실패하면 현행 화면을 그대로 보여준다(안전한 쪽이 기본) | `CONFIRMED` |
| 인지 수단 | 3안 검토됨 · **확정 없음** | `OPEN` |
| **현재 화면 변형** | **0건** | `CONFIRMED` |

**화면 변형이 0건인 경위 (사실 기록)**

M-01 회원가입 링크 억제 1건이 유일한 변형이었으나, 그 전제였던 CH-61(M-01 가입 동선 병기)이 **CH-64로 철회**되면서 억제할 대상이 사라졌다. 회원가입 경로는 전체메뉴에만 존재하며, 전체메뉴는 사용자가 스스로 열어 찾는 곳이므로 항목을 지우면 그 자체가 마찰이 된다.

**기각된 변형 (재론 금지 — 사유 포함)**

| 검토안 | 기각 사유 | 상태 |
|---|---|---|
| 로그인 영역 강조 확대 | 복귀 목적이 로그인이라는 관측이 없다 | `UNVERIFIED` |
| 개통 우선 배치 | 동일 | `UNVERIFIED` |
| 완료 안내 문구 노출 | 동일 | `UNVERIFIED` |
| 전체메뉴 가입 항목 억제 | 찾은 항목이 없으면 그 자체로 마찰 | `CONFIRMED` (기각) |
| M-10 팝업 억제 | 방향은 타당하나 현행 팝업의 내용·조건·주기 미확인 — **조건부 제안으로만 유지** | `OPEN` · DV-03 |

**조건 · 예외 (확정)**

| 상황 | 처리 | 상태 |
|---|---|---|
| 리퍼러 유실 · 판정 실패 | 일반 진입으로 처리 | `CONFIRMED` |
| 새로고침 · 뒤로가기 재진입 | 맥락이 해제된다. **현재는 화면 변형이 없어 사용자에게 보이는 차이도 없다** | `CONFIRMED` |
| 앱 웹뷰 진입과 동시 발생 | **앱 맥락이 우선한다** | `CONFIRMED` |
| 접근 경로 손실 | 없다 — 회원가입 경로는 전체메뉴에 그대로 있다 | `CONFIRMED` |

**미확정**

- 복귀 목적 — 관측되지 않았다 `UNVERIFIED`. ME-01이 붙기 전에는 화면 변형을 확정하지 않는다
- `/util/join/joinComplete`의 실제 내용 — 확인하지 않았다. "계정 생성 완료"라는 해석이 뒤집히면 대상도 바뀐다 **`CONDITIONAL`**

> **추가 UX를 만들지 않는다.** 이 절은 정의와 현재 상태(변형 0건)의 기록이다.

### 4-3. 앱 WebView 진입

| 항목 | 내용 | 상태 |
|---|---|---|
| 진입 조건 | `?userAgent=aos` / `?userAgent=ios` | PHASE 6 STATE `CONFIRMED` |
| 관측 규모 | **3,494 sessions** (GA4 리퍼러) | `CONFIRMED` (관측값) |
| PHASE 6 기록 | "진입 맥락에 맞는 처리 **(상세 설계)**" | `CONFIRMED` |
| 구체 처리 내용 | **정의되지 않았다** | **`OPEN`** |
| 프로토타입 구현 | **없음** (`userAgent` 관련 코드 0건) | **`SPEC GAP` · 미착수** |
| RS-01과 동시 발생 시 | 앱 맥락 우선 | `CONFIRMED` |

> 3,494 sessions는 **규모 관측값**이며 구현 요구사항이 아니다. 어떤 처리를 할 것인지는 원자료에 없다 — **처리 방식을 새로 설계하지 않는다.**

### 4-4. 로그인 오류 복귀

| 항목 | 내용 | 상태 |
|---|---|---|
| 진입 조건 | `/error?url=…api/login/after` | PHASE 6 STATE `CONFIRMED` |
| 관측 규모 | **1,128 sessions** | `CONFIRMED` (관측값) |
| PHASE 6 기록 | "**오류 상태를 유지한 채 재시도 경로 제공**" | `CONFIRMED` (원칙) |
| 구체 처리 내용 | 정의되지 않았다 | **`OPEN`** |
| 프로토타입 구현 | **없음** | **`SPEC GAP` · 미착수** |
| ME-03 연계 | 복귀 → 로그인 성공 측정 시 RS-01과의 중복 여부를 함께 본다 | 제안 · `OPEN` |

> 기존 자료에 있는 경로(`/error?url=…api/login/after`)만 사용한다. 오류 화면 UX·재시도 버튼 형태·문구를 새로 설계하지 않는다.

---

## 05. 상태 전이

### 5-1. 프로토타입에 구현된 전이

`PROTOTYPE-ONLY` — 전이의 **존재**는 확인되나 실제 서비스 구현 방식이 아니다.

```
M-03   default ──선택──> selected ──> 목적지 이동(pricList)
       조회 실패 ──재시도──> 재조회 ──> default
       default ⇄ 가격 미수신          (행·이동 유지)

M-04   default ⇄ playing ⇄ paused
       default ──swipe/dot/prev·next──> default(다른 슬라이드)
       loading ──> default
       등록 0건 ──> 영역 미노출

M-05   개통 가능 ⇄ 개통 불가          (인라인 문구·CTA disabled 전환)

M-06   collapsed ──펼침──> loading ──> expanded
       expanded ──접힘──> collapsed
       expanded ──탭 전환──> expanded(다른 탭)

오버레이 closed ──열기──> open ──ESC/닫기──> closed(호출 지점 복귀)

내비   메인 ──이동──> 목적지 ──복귀──> 메인(스크롤·선택·접힘 복원)
```

### 5-2. 명세에 필요하나 전이가 확인되지 않은 것

| 전이 | 상태 | 사유 |
|---|---|---|
| `M-06 loading → 펼침 실패` | **`SPEC GAP`** | 원칙(SP-2)은 확정. **실패 판정 조건·재시도 방식·표시 위치는 원자료에 없다** `UNVERIFIED` |
| `정상 → 전역 네트워크 오류` | **`SPEC GAP`** | 원칙(SP-3)은 확정. **오류 감지 시점·복구 전이는 원자료에 없다** `UNVERIFIED` |
| `M-03 조회 실패 → 전역 오류` 관계 | `UNVERIFIED` | 두 상태의 우선순위가 정의되지 않았다 |
| `일반 진입 → WebView 진입` | **`SPEC GAP`** | 처리 내용 미정의 |
| `일반 진입 → 로그인 오류 복귀` | **`SPEC GAP`** | 처리 내용 미정의 |
| `RS-01 진입 → 화면 변형` | 해당 없음 | **현재 변형 0건**이므로 전이가 존재하지 않는다 |
| `목록 → 메인 복귀 시 선택 유지` | `UNVERIFIED` | TR-03B — 원자료가 "이번 범위 밖"으로 명시 |
| `뒤로가기·새로고침 후 조건 유지` | `UNVERIFIED` | TR-03A |

> 위 항목의 전이 조건과 결과를 **임의로 확정하지 않는다.**

---

## 06. Prototype 미구현 상태 (SPEC GAP)

프로토타입에 없다고 해서 요구사항을 삭제하지 않는다.

| # | 상태 | 근거 | 프로토타입 | 처리 |
|---|---|---|---|---|
| G-1 | **전역 네트워크 오류** | PHASE 7-1 (SP-3) | 없음 | 명세로 기록 · 프로토타입에 추가하지 않음 |
| G-2 | **M-06 펼침 실패** | PHASE 7-1 (SP-2) | 없음 | 명세로 기록 |
| G-3 | **앱 WebView 진입 상태** | PHASE 6 STATE | 없음 | 명세로 기록 · 처리 내용은 `OPEN` |
| G-4 | **로그인 오류 복귀 상태** | PHASE 6 STATE | 없음 | 명세로 기록 · 처리 내용은 `OPEN` |

> `G-1`~`G-4`는 **문서 내부 목록 번호**이며 프로젝트 ID 체계에 편입하지 않는다.

---

## 07. Prototype-only 항목 (요구사항 아님)

| 항목 | 성격 |
|---|---|
| 상태 객체 `S` · 메모리 전용 보관 | 검증 구현 |
| `paneMain` / `panePric` / `paneExt` pane 전환 | 실제는 페이지 이동 |
| 검증 도구 `devToggle` · `drawer` · 상태 강제 토글(`data-m3` 등) | 검증 장치 |
| 인터랙션 로그 21종 | QA용 콘솔 로그 — **측정 이벤트 아님** |
| SAMPLE 데이터 `PLANS` · `SLIDES` · `PHONES` | 구조 검증용 |
| 셀프개통 시간 `09:00 ~ 20:00` | **결정 문서에 없음** — 표시 예시 |
| 오류·빈 상태 문구 전체 | 카피 미확정 (OI-05 계열) |
| `scrollTop > 4` 등 임계값 | 구현 세부 |
| 자동재생 주기 · 전환 시간 | 구현 세부 |
| GNB 목적지 16개 | 실제 GNB 구조 `UNVERIFIED` |
| M-06 탭 라벨 | 프로토타입 "0원 스마트폰 / 반값 안드로이드 / 반값 아이폰" vs PHASE 6 "인기 0원폰 / 반값 갤럭시 / 반값 아이폰" — **문서 간 표기 상이** `UNVERIFIED` |

---

## 부록 A. TC — 상태·예외 검증

| TC ID | 검증 대상 | 기대 | 근거 | 현재 |
|---|---|---|---|---|
| TC-03-01 | M-03 `default` → `selected` | 단일 선택 · 다른 행 선택 해제 · 목적지 이동 발생 | DD-07 · PHASE 7-1 D | 검증 가능 |
| TC-03-02 | M-03 `조회 실패` → `retry` | 재시도 시 재조회 후 `default` 복귀 | CH-21 | 검증 가능 — **요구사항 확정은 NEEDS CONFIRMATION** |
| TC-03-03 | `가격 미수신` 상태에서 행 동작 | 5행 유지 · 가격 칸만 빔 · **이동 가능** · 오류 문구 없음 | PHASE 7-1 | 검증 가능 |
| TC-03-04 | M-06 `collapsed` → `loading` → `expanded` | 접힘 기본 · 펼칠 때 로드 | DD-11 | 검증 가능 |
| TC-03-05 | M-06 `펼침 실패` | 해당 섹션만 재시도 · 화면 전체 오류 아님 | SP-2 | **검증 불가 — 개발 확인 필요** (프로토타입 미구현) |
| TC-03-06 | M-05 `개통 불가` 인라인 처리 | CTA `disabled` + 인라인 사유·시간 · **팝업 없음** | DD-09 · UI-P3 | 검증 가능 (시간 값은 제외) |
| TC-03-07 | 전역 네트워크 오류에서 P0 렌더 | M-01·M-02·M-03 렌더 유지 · 오류는 실패 영역에만 | SP-3 | **검증 불가 — 개발 확인 필요** |
| TC-03-08 | M-04 `등록 0건` | 영역 전체 미노출 · 빈 상태 문구 없음 | PHASE 7-1 · PHASE 6 F | 검증 가능 |
| TC-03-09 | M-04 `loading` | 배너 1장 높이 확보 · 레이아웃 시프트 없음 | PHASE 7-1 | 검증 가능 |
| TC-03-10 | 오버레이 공통 | 배경 스크롤 잠금 · 포커스 트랩 · ESC · 호출 지점 복귀 | PHASE 7-1 G | 검증 가능 |
| TC-03-11 | 메인 복귀 시 스크롤·접힘 복원 | 복원됨 | PHASE 7-1 Navigation | **부분 검증** — 수단 미확정(TR-03A/B) |
| TC-03-12 | 같은 세션 재진입 시 접힘·닫힘 유지 | 유지됨 | PHASE 7-1 Navigation | **검증 불가 — 수단 미확정** |
| TC-03-13 | M-10 진입 시 동작 | **판정 기준 없음** | DV-03 | **검증 불가 — 규칙 미확정** |
| TC-03-14 | 검색 실행 | **판정 기준 없음** | D-09(5.2) · DV-02 | **검증 불가 — 미구현** |
| TC-03-15 | RS-01 진입 | 화면 변형 없음 · 일반 진입과 동일 | §4-2 | 검증 가능 (현 상태 확인) |
| TC-03-16 | 판정 실패 시 기본값 | 일반 진입으로 처리 | §4-2 | 검증 가능 |
| TC-03-17 | WebView 진입 | **판정 기준 없음** | §4-3 | **검증 불가 — 미착수** |
| TC-03-18 | 로그인 오류 복귀 | **판정 기준 없음** | §4-4 | **검증 불가 — 미착수** |
| TC-03-19 | FIRST / REVISIT 화면 동일성 | 두 URL의 화면 구성 동일 | §4-1 | 검증 가능 |

---

## 부록 B. 미확정 이월

기존 ID를 유지한다.

| ID | 항목 | 상태 | 해소 경로 |
|---|---|---|---|
| **DV-01** | `#phoneArea` 측정 태깅 | `UNVERIFIED` | 태깅 후 재판정 · 태깅 전 M-06 확대 금지 |
| **DV-03** | 팝업 개수·노출 규칙·재노출 주기 | `OPEN` | 운영/CMS 확인 (GA4 불가) |
| **DV-06** | 푸터 '영역 보기' 클릭 목적 | `UNVERIFIED` | 세션 녹화 확인 |
| **TR-03A** | 뒤로가기·새로고침 후 조건 유지 | `UNVERIFIED` | 개발 확인 |
| **TR-03B** | 목록→메인 복귀 시 선택 구간 유지 | `UNVERIFIED` | 상세 설계 단계 (범위 밖) |
| **TR-04** | 초기화 로직 연결 | `OPEN` | 개발 확인 |
| **OI-07** | sticky 헤더 세로 공간 부작용 | `UNVERIFIED` | 실측 후 재검토 |
| **RS-01** | 복귀 목적 · 인지 수단 · joinComplete 내용 | `UNVERIFIED` / `OPEN` / `CONDITIONAL` | ME-01 선행 |
| **DC-03** | M-07 운영 슬롯 규칙 | `OPEN` | 제휴영업팀 협의 |
| — | 앱 WebView 진입 처리 | `OPEN` · `SPEC GAP` | PHASE 6 "상세 설계" 이행 |
| — | 로그인 오류 복귀 처리 | `OPEN` · `SPEC GAP` | 동일 |
| — | M-06 펼침 실패 동작 | `SPEC GAP` | 원칙만 확정 · 동작 미정의 |
| — | 전역 네트워크 오류 감지·복구 전이 | `SPEC GAP` | 원칙만 확정 |
| — | M-03 조회 실패와 SP-3의 관계 | `UNVERIFIED` | 확인 필요 (본 명세에서 신규 발견) |
| — | M-06 탭 라벨 문서 간 표기 상이 | `UNVERIFIED` | 확인 필요 (본 명세에서 신규 발견) |
| — | 셀프개통 가능 시간 값·판정 주체 | `UNVERIFIED` | SPEC-04 DS-5 · DQ-11 |

**본 명세에서 새로 만든 것**

| 항목 | 성격 |
|---|---|
| `TC-03-01` ~ `TC-03-19` | 검증 항목 ID (신규) |
| `SP-1` ~ `SP-5` · `G-1` ~ `G-4` | **문서 내부 목록 번호** — 프로젝트 ID 체계에 편입하지 않음 |

새로운 UX 결정 ID · 상태 정책 ID는 만들지 않았다.

---

## SPEC-04와의 정합

| 항목 | SPEC-04 | SPEC-03 | 충돌 |
|---|---|---|---|
| TR-01 · TR-02 | 사실 확정 | 인용만 | 없음 |
| TR-03A · TR-03B | `UNVERIFIED` | `UNVERIFIED` (§3-4 · §5-2) | 없음 |
| TR-04 | `OPEN` | 인용만 | 없음 |
| 가격 미수신 | 데이터 계약 요구 | UI 상태 정의 | 없음 — 관점 분리 |
| SAMPLE / 운영 데이터 경계 | §05 | §07 | 없음 |
| 상태 지속(저장소) | 수단 `UNVERIFIED` | **요구는 `CONFIRMED`** · 수단 `UNVERIFIED` | 없음 — §3-4에 관계 명시 |
| 셀프개통 시간 | DS-5 `UNVERIFIED` | `PROTOTYPE-ONLY` | 없음 |

---

기준 · PHASE 5.2 확정 결정 · PHASE 6 화면 구조 / STATE / F Visibility · PHASE 7-1 UI 설계(상태 원칙 · Navigation · 접근성) · PHASE 7-4 Prototype · PHASE 8 GAP Analysis · SPEC_PREFLIGHT · SPEC-04
미확정 이월 · DV-01 · DV-03 · DV-06 · TR-03A · TR-03B · TR-04 · OI-07 · RS-01 · DC-03 · WebView · 로그인 오류 복귀 · M-06 펼침 실패 · 전역 네트워크 오류
본 명세는 확정된 상태 원칙과 관측 사실만을 기술한다. 오류 UX·팝업 규칙·검색 실행·WebView 처리·저장 방식은 이 문서에서 정하지 않는다.
