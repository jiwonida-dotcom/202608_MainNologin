# SPEC-02 · Functional Specification

U+유모바일 비로그인 모바일 메인 개편 · Development Specification
작성 2026-08-27 · 기준 커밋 `beea5bb`
선행 · `SPEC-03_State_Exception.md` (상태) · `SPEC-04_Data_Handoff.md` (데이터·전달)

**상태 표기** — `CONFIRMED` / `PARTIAL` / `UNVERIFIED` / `OPEN` / `CONDITIONAL` / `PROTOTYPE-ONLY` / `OUT OF SCOPE` / `SPEC GAP` / `DEVELOPMENT CONFIRMATION REQUIRED`

---

## 00. 이 문서의 범위

| 다루는 것 | 다루지 않는 것 (참조처) |
|---|---|
| 기능이 무엇을 하고 어떻게 동작하는가 | 상태의 상세 정의 → **SPEC-03** |
| 사용자 행동 → 시스템 동작 → 결과 | 파라미터·데이터 계약 → **SPEC-04** |
| 기능 단위의 예외 진입점 | 측정 이벤트 사양 → **SPEC-05**(예정) |
| 기능별 접근성 요건 | 화면 치수·레이아웃 → **SPEC-01**(예정) |

**공통 전제 (전 기능 적용 · 재기술하지 않음)**

| 전제 | 근거 | 상태 |
|---|---|---|
| 적용 대상은 `/nologfirst` · `/nologrevisit` 두 URL이며 **화면 차등이 없다** | PHASE 5.2 · PHASE 6 | `CONFIRMED` |
| 운영 단위 병합 여부는 미결 — **기능 확정과 무관** | D-01(FR-07) | `OPEN` |
| M-01~M-11의 구조·순서·개수를 변경하지 않는다 | PHASE 6 · PHASE 7-1 | `CONFIRMED` |
| 하단 고정 CTA를 두지 않는다 | PHASE 7-1 Navigation | `CONFIRMED` |
| 신규 영역을 추가하지 않는다 | DD-14 | `CONFIRMED` |
| 프로토타입의 인터랙션 로그 21종은 **QA용이며 운영 측정 이벤트가 아니다** | §13 | `PROTOTYPE-ONLY` |
| 프로토타입의 pane 전환은 실제로는 **페이지 이동**이다 | §13 | `PROTOTYPE-ONLY` |

---

## F-01 · QuickMenu 이동

#### 1. 목적
콘텐츠를 읽지 않고 목적지로 바로 점프한다. (PHASE 6 M-02 원문)

#### 2. 적용 범위
M-02 · C-02 QuickItem · 3항목.

#### 3. 선행 조건
상시 노출. 조건 없음. (PHASE 6 M-02 "노출 조건 — 상시")

#### 4. 사용자 행동
3항목 중 하나를 탭한다.

#### 5. 시스템 동작
해당 항목의 목적지로 이동한다. 조건·파라미터를 부가하지 않는다.

#### 6. 결과

| 순서 | 항목 | 목적지 | 근거 |
|---|---|---|---|
| ① | 전체 요금제 | `/product/pric/usim/pricList` | PHASE 6 E |
| ② | 휴대폰 | `/shop/mobile/mobileJoinIntro` | PHASE 6 E |
| ③ | 셀프개통 | `/shop/self/openingGuide` | PHASE 6 E |

- 배치 순서 = **실사용 순위**. Clarity 동일 조회 클릭 세션 순위 3,541 > 3,064 > 1,572 > 774 (분모 불요) `CONFIRMED`
- **이벤트 항목은 두지 않는다** — 4항목 중 최소 규모이고 목적지가 M-04 '전체보기'와 완전 동일 (D-02(5.2)) `CONFIRMED`
- ①의 라벨은 CH-35로 "요금제 찾기" → **"전체 요금제"**로 변경됐다. **구성·순서·목적지는 불변**이며 라벨은 OI-05 카피 영역 `CONDITIONAL`

#### 7. 예외 / 실패 상태
정의된 예외 없음. → SPEC-03 §02 M-02

#### 8. 데이터 / 전달
조건 전달 없음. ①은 **조건 없는 전체 목록 진입**이며 F-03의 대상이 아니다.

#### 9. 접근성
터치 타깃 44×44 · 인접 간격 8 · 포커스 링 유지 · DOM 순서 = 화면 순서. (PHASE 7-1 G) `CONFIRMED`
3등분 그리드는 어떤 폭에서도 2행으로 접히지 않는다 — 라벨을 줄인다. (PHASE 7-1 E) `CONFIRMED`

#### 10. 측정
DV-05 — M-02 각 항목과 M-03 각 구간의 클릭을 **동일 단위(세션당 클릭)로 수집해 비교**한다. CH-35 라벨 변경 효과도 이 측정에 포함한다. 운영 이벤트 사양 **미확정** → SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`** |
| 근거 ID | D-01(5.2) · D-02(5.2) · DD-02 · CH-35 |
| 참조 | PHASE 6 M-02 · E · PHASE 7-1 C-02 |
| 미확정 | 라벨 문구(OI-05) |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-01 | 3항목 존재·순서 | 전체 요금제 → 휴대폰 → 셀프개통 | 가능 |
| TC-02-02 | 각 항목 목적지 | 위 표와 일치 | 가능 |
| TC-02-03 | 이벤트 항목 부재 | 항목 0건 | 가능 |

---

## F-02 · M-03 구간 선택

#### 1. 목적
내 기준(데이터·가격)으로 요금제를 좁혀 목록으로 이동한다. (PHASE 6 M-03 원문)

#### 2. 적용 범위
M-03 · C-03 DataRangeRow × 5.

#### 3. 선행 조건
상시 노출. 운영 on/off 대상이 아니다. (PHASE 6 M-03) `CONFIRMED`
구간 경계는 **정적 값**이므로 로딩이 없다. (PHASE 7-1) `CONFIRMED`

#### 4. 사용자 행동
5개 행 중 하나를 탭한다. **행 전체가 탭 영역**이며 가격 텍스트도 포함된다. (PHASE 7-1 C-03)

#### 5. 시스템 동작
1. 선택한 구간을 **단일 선택**으로 표시한다 (기존 선택 해제)
2. 목록 화면으로 이동한다 — F-04
3. 선택 조건을 목적지에 전달한다 — F-03

메인 안에서 결과를 갱신하지 않는다. (PHASE 7-1 D) `CONFIRMED`

#### 6. 결과
목록 페이지로 이동하며, 선택 구간이 **적용된 상태**로 도착하는 것이 확정 UX다. 실제 선적용 여부는 F-03 참조.

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 M-03. 요약: `가격 미수신`(가격 칸만 비우고 **행과 이동은 유지**) · `조회 실패`/`retry`(CH-21 · `PARTIAL`).

> `조회 실패` 시 전 구간을 `aria-disabled`로 두는 처리와 "P0는 항상 렌더"(PHASE 7-1) 원칙의 관계는 원자료에 판단이 없다. `UNVERIFIED`

#### 8. 데이터 / 전달
구간 5종·경계값·대표 가격 → **SPEC-04 §02 · §04**. 대표 가격의 정의(최저가 / 중위가)는 문서 간 상이 `UNVERIFIED`.

#### 9. 접근성

| 요건 | 근거 |
|---|---|
| 행 전체가 **하나의 링크로 읽힌다** — "데이터 3GB 이하, 8,900원부터, 요금제 목록". 라벨과 가격이 두 노드로 분리되지 않는다 | PHASE 7-1 G `CONFIRMED` |
| 터치 타깃 44×44 | PHASE 7-1 G `CONFIRMED` |
| 텍스트 200% 확대 시 행이 2줄이 되는 것은 허용하되 **5행이 모두 존재하고 탭 가능**해야 한다 | PHASE 7-1 E `CONFIRMED` |
| 어떤 폭에서도 5행 동시 노출 — 접거나 가로 스크롤로 바꾸지 않는다 | DD-04 · PHASE 7-1 E `CONFIRMED` |

#### 10. 측정
구간별 진입은 **새로운 측정 축**이 된다 (PHASE 7-1). DV-05 동일 단위 비교 대상. 운영 이벤트 사양 미확정 → SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`** (구조·동작) |
| 근거 ID | D-04(5.2) · DD-04 · DD-07 · CH-21(예외만 `PARTIAL`) |
| 참조 | PHASE 5.1 결정 1 · PHASE 6 M-03 · PHASE 7-1 C-03 · D · G |

**금지 사항 (확정)** — 추천·개인화 정렬, 메인 내 필터 적용·결과 갱신, 결과 개수·미리보기, 다중 선택, 특정 구간만 강조. (DD-07 · PHASE 7-1 D · F) `CONFIRMED`

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-04 | 5행 동시 노출 | 360/390/430/480 전 폭에서 5행 | 가능 |
| TC-02-05 | 단일 선택 | 다른 행 선택 시 이전 선택 해제 | 가능 |
| TC-02-06 | 접근가능 이름 | 라벨+가격+목적이 한 문장 | 가능 |
| TC-02-07 | 금지 요소 부재 | 체크박스·결과 개수·추천 정렬 0건 | 가능 |

---

## F-03 · 조건 전달

#### 1. 목적
메인에서 고른 구간을 목적지 목록에 조건으로 전달해, **조건이 적용된 목록**에 도착하게 한다.

#### 2. 적용 범위
M-03 구간 선택 → `/product/pric/usim/pricList` 진입 구간.

#### 3. 선행 조건
F-02의 구간 선택이 발생했을 것.

#### 4. 사용자 행동
구간 행 탭 (F-02와 동일 행동).

#### 5. 시스템 동작 — **두 사실을 분리해 기술한다**

**사실 ① — 조건 전달 구조가 존재한다** `CONFIRMED`

| 항목 | 내용 |
|---|---|
| 전달 수단 | **쿼리스트링** (TR-02) |
| 운영 필터 체계 | `dan1` ~ `dan7` · `array` |
| 데이터 구간 | `dan2 = 0~4` 가 M-03 5구간과 **1:1 대응** |
| 파서 | `_getParameterByName(name, url)` 가 이미 존재하며 **정상 파싱한다** |

**사실 ② — 전달된 조건이 초기 상태에 선적용되지 않는다** `CONFIRMED`

| 항목 | 내용 |
|---|---|
| 실측 일자 | 2026-08-26 |
| 실측 방법 | 운영 `pricList`에 `?dan2=2&array=discntaddvat` 로 직접 진입 |
| 결과 | **전 항목 미체크 · 정렬 추천순 유지** |

> **"전달 불가"가 아니다.** 파라미터는 전달되고 파싱도 되지만, 그 결과가 UI 초기 상태에 연결되어 있지 않다. `dan2` 초기값 전달과 실제 초기 필터 적용은 **별개의 문제**다.

#### 6. 결과

| 확정 UX (구현 방식과 무관) | 근거 | 상태 |
|---|---|---|
| 조건이 적용된 목록에 도착한다 | PHASE 7-1 D | `CONFIRMED` (목표) |
| 기본 정렬은 가격 낮은 순 | PHASE 7-1 D · 정렬 91.4% | `CONFIRMED` (목표) |
| 목록 화면에 적용된 조건이 표시된다 | PHASE 7-1 D | `CONFIRMED` (목표) |
| **현재 실제 결과** | 실측 | **조건 미적용 · 정렬 추천순** `CONFIRMED` |

#### 7. 예외 / 실패 상태
**TR-01이 불가로 확정될 경우의 대응 원칙** (PHASE 7-1 원문 승계 · 새 대안을 만들지 않는다)

> "M-03의 구조는 바꾸지 않는다. 구간 선택은 유지하고 조건 전달만 대안으로 처리한다. 최악의 경우에도 현행(조건 없이 목록 이동)보다 나쁘지 않으며, 구간별 진입 자체가 새로운 측정 축이 된다. **단, 이 경우 DD-05의 기대 효과를 축소해 기술해야 한다.**"

#### 8. 데이터 / 전달
→ **SPEC-04 §03**. 미확정 구분:

| ID | 항목 | 상태 |
|---|---|---|
| TR-01 | 선적용 여부 | **`CONFIRMED`(선적용되지 않음)** |
| TR-02 | 전달 수단 | **`CONFIRMED`(쿼리스트링·파서 존재)** |
| TR-04 | 파싱 결과를 초기화 로직에 연결 | **`OPEN` · DEVELOPMENT CONFIRMATION REQUIRED** |
| — | `array` 의 의미·필수 여부 | `UNVERIFIED` |
| — | `dan1`·`dan3`~`dan7` 의 의미 | `UNVERIFIED` |
| — | **정렬 초기값 지정 파라미터의 존재 여부** | `UNVERIFIED` |

> 실측이 좁힌 범위는 "**파싱 결과를 초기화 로직에 연결하는 작업만 남는다**"까지다. 연결 방식·영향 범위·작업량은 정하지 않는다.

#### 9. 접근성
해당 없음 (전달은 비시각 동작). 도착 화면의 조건 표시 접근성은 목록 화면 소관 `OUT OF SCOPE`.

#### 10. 측정
프로토타입의 `HANDOFF` 로그는 **QA용**이며 운영 이벤트가 아니다. `PROTOTYPE-ONLY`

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`PARTIAL`** — UX 확정 · 전달 구조 확인 · **선적용 미지원** |
| 근거 ID | DD-05 · TR-01 · TR-02 · TR-04 · OI-02 |
| 참조 | PHASE 7-1 D · SPEC-04 §03 · PHASE 7-4 실측 기록 |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-08 | 구간 → `dan2` 생성 | `r1→0 … r5→4` | 가능 |
| TC-02-09 | 쿼리스트링 파싱 | 정상 파싱 | 가능 |
| TC-02-10 | 초기 필터 선적용 | **현재 기대 = 선적용 안 됨** | 가능(현 상태 재현) |
| TC-02-11 | 초기 정렬 적용 | **현재 기대 = 추천순 유지** | 가능(현 상태 재현) |
| TC-02-12 | 연결 후 선적용 | 조건 적용 목록 도착 | **검증 불가 — 개발 확인 필요**(TR-04) |

---

## F-04 · 목록 진입

#### 1. 목적
요금제 목록 페이지로 이동한다.

#### 2. 적용 범위
M-02 ①(조건 없는 진입) · M-03(조건 동반 진입).

#### 3. 선행 조건
없음(M-02) / 구간 선택(M-03).

#### 4. 사용자 행동
QuickMenu ① 탭 또는 M-03 구간 행 탭.

#### 5. 시스템 동작
`/product/pric/usim/pricList` 로 이동한다.

#### 6. 결과

| 진입 경로 | 목적지 | 조건 |
|---|---|---|
| M-02 ① 전체 요금제 | `/product/pric/usim/pricList` | 없음 |
| M-03 구간 선택 | `/product/pric/usim/pricList` | F-03 |

**DD-15 — 메인 → `pplrPpn` 직결 폐기**

| 항목 | 내용 | 상태 |
|---|---|---|
| 결정 | 메인에서 `pplrPpn`으로 직결하는 경로를 폐기한다. **페이지 자체는 유지한다** | `CONFIRMED` |
| 근거 | 목적지 클릭률 `pricList` 27.9%(20,837/74,812) vs `pplrPpn` 9.3%(1,185/12,772) · 신청서 기여 16,138 vs 706 (동일 기간·동일 도구) | `CONFIRMED` |
| 현재 위치 | `pplrPpn`은 **전체메뉴(M-11)에만 존재**한다 | `CONFIRMED` |

> 폐기 대상은 **메인의 직결 경로**이지 페이지가 아니다. 두 가지를 혼동하지 않는다.

#### 7. 예외 / 실패 상태
목록 결과 0건 등 목록 화면 내부 상태는 `OUT OF SCOPE`.

#### 8. 데이터 / 전달
→ F-03 · SPEC-04 §03. 목록 페이지의 **내부 구현은 확인된 범위만** 기술한다 — 필터 체계 `dan1~dan7`·`array` 존재, `dan2` 대응, 파서 존재, 선적용 안 됨. 그 외 내부 구조는 `UNVERIFIED`.

#### 9. 접근성
이동 자체에 별도 요건 없음. 진입점의 요건은 F-01 · F-02.

#### 10. 측정
`pricList` 진입은 기존 GA4 경로 데이터로 관측 가능. 신규 이벤트 필요 여부는 SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`** (진입 경로·목적지) |
| 근거 ID | DD-05 · DD-15 · D-06(5.2)(목적지 구조) |
| 참조 | PHASE 5.2 D · PHASE 6 E |
| 범위 밖 | 목록 페이지 내부 설계 · 보조 필터 순서(D-05(5.2) · DD-06) |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-13 | 목적지 경로 | `/product/pric/usim/pricList` | 가능 |
| TC-02-14 | 메인에 `pplrPpn` 직결 부재 | 메인 모듈 내 0건 | 가능 |

---

## F-05 · 뒤로가기 / 재진입

#### 1. 목적
되돌아오는 행동에서 사용자가 있던 자리를 잃지 않게 한다.
근거 — Quick back 54.8% / 39.3% "되돌아오는 행동 자체가 흔하다" (PHASE 7-1) `CONFIRMED`

#### 2. 적용 범위
메인 화면 전체 · C-08 접힘 컴포넌트 · M-03 선택 상태.

#### 3. 선행 조건
메인에서 이동이 발생한 뒤 복귀할 것.

#### 4. 사용자 행동
브라우저 뒤로가기 / 화면 내 복귀 / 같은 세션 재진입 / 새로고침.

#### 5. 시스템 동작 — **요구는 확정, 수단은 미확정**

| # | 요구 | 근거 | 요구 상태 | 수단 |
|---|---|---|---|---|
| ① | 메인 복귀 시 **스크롤 위치 복원** | PHASE 7-1 Navigation | `CONFIRMED` | `UNVERIFIED` |
| ② | 메인 복귀 시 **접힘 상태(C-08) 유지** | PHASE 7-1 Navigation | `CONFIRMED` | `UNVERIFIED` |
| ③ | **같은 세션 재진입 시 접힘·닫힘 상태 유지** | PHASE 7-1 Navigation | `CONFIRMED` | `UNVERIFIED` |

#### 6. 결과
위 ①~③이 충족된 메인 화면.

#### 7. 예외 / 실패 상태
→ SPEC-03 §3-4. 판정 실패·상태 유실 시의 처리는 원자료에 없다 `UNVERIFIED`.

#### 8. 데이터 / 전달 — **TR-03의 의미 충돌**

> **원 ID는 `TR-03` 하나다.** 현재 문서들에서 이 ID가 서로 다른 두 의미로 쓰이고 있어 명세 내부에서만 구분 표기한다. **`TR-03A` / `TR-03B`는 신규 요구사항 ID가 아니며, 원 ID `TR-03`을 삭제하거나 변경하지 않는다.**

| 표기 | 질문 | 출처 | 상태 |
|---|---|---|---|
| **TR-03A** | 조건이 적용된 상태가 **뒤로가기·새로고침** 후에도 유지되는가 | PHASE 7-1 | `UNVERIFIED` |
| **TR-03B** | **목록 → 메인 복귀 시 선택 구간**이 유지되는가 | `alignment.html` 15절 — "상세 설계 단계에서 정의(**이번 범위 밖**)" | `UNVERIFIED` · `OUT OF SCOPE`(현 단계) |

**지속 정책은 확정하지 않는다** — 저장소 사용 여부·방식은 `UNVERIFIED` (SPEC-04 §07 · DQ-13). RS-01은 "확장하려면 저장소가 필요하고 **그 필요를 정당화할 관측이 없다**"로 확장을 명시적으로 배제했으므로, RS-01을 근거로 저장소를 도입하지 않는다.

#### 9. 접근성
복귀 시 포커스 처리 요건은 원자료에 없다 `UNVERIFIED`. 오버레이 닫기 후 호출 지점 복귀는 F-10 · F-12 소관.

#### 10. 측정
프로토타입 `BACK` 로그는 QA용 `PROTOTYPE-ONLY`.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`PARTIAL`** — 요구 확정 · 수단 미확정 |
| 근거 ID | TR-03(A/B 표기) · TR-04 |
| 참조 | PHASE 7-1 Navigation · SPEC-03 §3-4 · SPEC-04 §03 · §07 |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-15 | 메인 복귀 시 스크롤 복원 | 복원됨 | **부분** — 수단 미확정 |
| TC-02-16 | 메인 복귀 시 접힘 유지 | 유지됨 | **부분** |
| TC-02-17 | 재진입 시 접힘·닫힘 유지 | 유지됨 | **검증 불가 — 개발 확인 필요** |
| TC-02-18 | 새로고침 후 조건 유지 | **판정 기준 없음** | **검증 불가**(TR-03A) |

---

## F-06 · M-06 펼침 / 접힘

#### 1. 목적
성과가 검증되지 않은 영역을 **측정 가능한 구조로만** 두고, 초기 로딩 비용을 쓰지 않는다. (DD-11)

#### 2. 적용 범위
M-06 · C-08 CollapsibleSection.

#### 3. 선행 조건
**기본값은 접힘**(`collapsed`). (DD-11 · PHASE 7-1) `CONFIRMED`

#### 4. 사용자 행동
섹션 헤더를 탭한다. (헤더 전체가 탭 영역 — PHASE 7-1 C-08)

#### 5. 시스템 동작
`collapsed` → **펼칠 때 콘텐츠를 로드**(`loading`) → `expanded`.
재탭 시 `expanded` → `collapsed`.

#### 6. 결과
3탭 + 상품 카드가 노출된다(F-07). 접힘 상태는 복귀·재진입 시 유지된다(F-05 ②③).

#### 7. 예외 / 실패 상태

| 상태 | 확정 원칙 | 프로토타입 | 판정 |
|---|---|---|---|
| **펼침 실패** | "**해당 섹션만 재시도. 화면 전체 오류로 처리하지 않는다**" (PHASE 7-1) | **없음** | **`SPEC GAP` — 프로토타입 미구현** |

> 원칙은 확정이고 **구체 동작(실패 판정 조건·재시도 방식·표시 위치)은 원자료에 없다** `UNVERIFIED`. 프로토타입에 없는 상태를 구현 완료처럼 기술하지 않으며, 이 명세를 이유로 프로토타입에 추가하지도 않는다. → SPEC-03 §06 G-2

#### 8. 데이터 / 전달
휴대폰 상품 데이터는 **SAMPLE**이다(`PHONES` 12종). 실제 공급 방식 `UNVERIFIED` → SPEC-04 DS-4.

#### 9. 접근성
C-08에 `aria-expanded`. **화살표 방향만으로 상태를 전달하지 않는다.** (PHASE 7-1 G) `CONFIRMED`

#### 10. 측정
**DV-01 미해소** — `#phoneArea` 내 링크·버튼 13개 중 GTM 태깅 0개(PHASE 2 관측). 필요한 측정은 노출(impression) · 탭 전환 · 상품 카드 클릭 · 전체보기 4종(PHASE 6). **태깅이 붙기 전까지 영역을 확대하지 않는다.** `CONFIRMED`(제약)

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`**(접힘·펼침·지연 로드) / **`SPEC GAP`**(펼침 실패) |
| 근거 ID | D-03(5.2) · DD-11 · DV-01 · OI-08 |
| 참조 | PHASE 6 M-06 · PHASE 7-1 C-08 · G · SPEC-03 §02 |

**금지 사항 (확정)** — 강조·시각 비중 부여, 영역 확대, M-02의 휴대폰 수요를 M-06의 근거로 인용. `CONFIRMED`

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-19 | 초기 상태 | `collapsed` · `aria-expanded="false"` | 가능 |
| TC-02-20 | 펼칠 때 로드 | 접힘 상태에서 콘텐츠 미로드 | 가능 |
| TC-02-21 | 펼침 실패 | 해당 섹션만 재시도 | **검증 불가 — 개발 확인 필요** |

---

## F-07 · M-06 탭 전환

#### 1. 목적
휴대폰 영역의 큐레이션 3종 사이를 전환한다.

#### 2. 적용 범위
M-06 `expanded` 상태 내부의 3탭.

#### 3. 선행 조건
M-06이 `expanded` 일 것 (F-06).

#### 4. 사용자 행동
탭 탭(터치) 또는 **좌우 방향키**.

#### 5. 시스템 동작
선택 탭의 상품 카드 목록으로 교체하고, 새 선택 탭으로 포커스를 옮긴다.

#### 6. 결과
해당 큐레이션의 상품 카드가 노출된다. 구성은 **현행 유지**(3탭 + 상품 카드 + 전체보기)이며 확대하지 않는다. (PHASE 6 M-06) `CONFIRMED`

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 M-06.

#### 8. 데이터 / 전달
상품 데이터 `SAMPLE`. → SPEC-04 §05.

**탭 라벨 — 원자료 간 불일치 · 확정하지 않는다**

| 출처 | 라벨 |
|---|---|
| PHASE 6 M-06 | 인기 0원폰 / 반값 갤럭시 / 반값 아이폰 |
| PHASE 7-4 Prototype | 0원 스마트폰 / 반값 안드로이드 / 반값 아이폰 |

> **`UNVERIFIED`** — 어느 쪽도 선택하지 않는다. 라벨 확정은 카피 단계(OI-05 계열).

#### 9. 접근성
`role="tablist"` / `role="tab"` · `aria-selected` · **roving tabindex**(선택 탭만 `0`) · 좌우 방향키 이동 · 44px 타깃. (PHASE 7-1 G · 프로토타입 구현 확인) `CONFIRMED`

#### 10. 측정
DV-01의 "탭 전환(3탭)" — 어느 큐레이션이 쓰이는지. **미태깅** `UNVERIFIED`.
프로토타입 `PHONE_TAB` 로그는 QA용 `PROTOTYPE-ONLY`.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`**(구조·접근성) / **`UNVERIFIED`**(라벨) |
| 근거 ID | DD-11 · DV-01 · OI-08 |
| 참조 | PHASE 6 M-06 · PHASE 7-1 G |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-22 | 3탭 존재·ARIA | `role=tab` · `aria-selected` 1개만 true | 가능 |
| TC-02-23 | 키보드 전환 | 좌우 방향키로 순환·포커스 이동 | 가능 |
| TC-02-24 | 탭 라벨 | **판정 기준 없음** | **검증 불가 — 라벨 미확정** |

---

## F-08 · M-04 캐러셀

#### 1. 목적
지금 받을 수 있는 혜택을 확인하고 해당 이벤트로 바로 간다. (PHASE 6 M-04)

#### 2. 적용 범위
M-04 · C-05 BannerCarousel · 전체보기.

#### 3. 선행 조건
**운영 등록 배너가 존재할 것.** 0건이면 영역 미노출. (PHASE 6 F · PHASE 7-1) `CONFIRMED`

#### 4. 사용자 행동
배너 탭 · 좌우 스와이프 · 이전/다음 · 인디케이터 선택 · **자동 전환 정지/재생**.

#### 5. 시스템 동작
해당 슬라이드로 전환하고 현재 위치를 갱신한다. 배너 탭 시 해당 이벤트로 이동한다.

#### 6. 결과

| 대상 | 목적지 | 근거 |
|---|---|---|
| 개별 배너 | `/event-benefit/event/ongoing/{id}` | PHASE 6 E |
| 전체보기 | `/event-benefit/event/ongoing` | PHASE 6 E |

**우선순위 — P1** (시안 3순위 → P1 상향, DD-08) `CONFIRMED`
근거 — 이벤트 경로 8.4% / 15.8% · 기획전 배너 세션당 0.0303 / 0.0294(격차 3%) · 녹화에서 이벤트 상세 즉시복귀 0%(소표본).

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 M-04. 요약: `loading`(배너 1장 높이 확보 스켈레톤) · `등록 0건`(영역 전체 미노출 · **빈 상태 문구를 두지 않는다**).

#### 8. 데이터 / 전달
배너 데이터는 **SAMPLE**(`SLIDES` 3종). 배너 개수 상한·정렬 규칙은 원자료에 없다 `UNVERIFIED` → SPEC-04 DS-3.

#### 9. 접근성

| 요건 | 근거 |
|---|---|
| **자동 전환 정지 컨트롤 필수** | PHASE 7-1 G `CONFIRMED` |
| `prefers-reduced-motion` 에서 자동 전환 중지 | PHASE 7-1 G `CONFIRMED` |
| 현재 위치를 **텍스트로도 전달** | PHASE 7-1 G `CONFIRMED` |
| 배너 텍스트를 이미지에 굽지 않는다. 굽는 경우 동일 내용의 대체 텍스트 필수 | PHASE 7-1 G `CONFIRMED` |

#### 10. 측정
프로토타입 `CAROUSEL` · `M04_STATE` 로그는 QA용 `PROTOTYPE-ONLY`. 운영 측정은 기존 `ProductProBanner` 계열 관측이 있으나 신규 이벤트 사양은 미확정 → SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`**(구조·동작·접근성) |
| 근거 ID | DD-08 · R-02 |
| 참조 | PHASE 6 M-04 · E · F · PHASE 7-1 C-05 · G |

**금지 사항 (확정)** — 테두리 강조·모션 강조 추가. "운영 영역은 P0를 넘지 못한다"(UI-P4) `CONFIRMED`
자동 전환 주기·전환 시간은 `PROTOTYPE-ONLY`.

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-25 | 정지 컨트롤 존재·동작 | 자동 전환 중지됨 | 가능 |
| TC-02-26 | reduced-motion | 자동 전환 시작 안 함 | 가능 |
| TC-02-27 | 현재 위치 텍스트 | "n/N" 형태로 노출 | 가능 |
| TC-02-28 | 등록 0건 | 영역 미노출 · 문구 없음 | 가능 |

---

## F-09 · M-05 개통 상태 표시

#### 1. 목적
가입 방법을 확인하거나 바로 개통을 시작한다. 개통 불가 시간에는 그 사실을 **화면 흐름 안에서** 알린다.

#### 2. 적용 범위
M-05 · C-06 ActionCard · C-07 InlineNotice.

#### 3. 선행 조건
상시 노출. 시간대 안내는 **조건부 문구**. (PHASE 6 M-05) `CONFIRMED`

#### 4. 사용자 행동
가입 신청 탭 / 셀프개통 시작 탭.

#### 5. 시스템 동작

| 상태 | 동작 | 근거 |
|---|---|---|
| 개통 가능 | 정상 이동 | PHASE 6 M-05 |
| 개통 불가 | **C-06을 `disabled`로 두고 C-07 인라인으로 사유·가능 시간 명시. 팝업 금지** | PHASE 7-1 · DD-09 · UI-P3 `CONFIRMED` |

**DD-09 — 팝업 → 인라인 전환**
시안 R-09의 시간대 안내 **팝업을 인라인으로 전환**한다. 사유 — "팝업이 이미 최대 마찰 요인이므로 새 팝업을 추가하지 않는다"(P-A) `CONFIRMED`

#### 6. 결과

| 대상 | 목적지 | 근거 |
|---|---|---|
| 가입 안내 | `/shop/mobile/mobileJoinIntro` | PHASE 6 E · DD-10 |
| 셀프개통 | `/shop/self/openingGuide` | PHASE 6 E · DD-10 |

> 시안이 지정한 `/onsale/*` 2건은 **현행 실제 경로로 교체**됐다(DD-10) `CONFIRMED`

#### 7. 예외 / 실패 상태
개통 불가는 SP-4(빈 상태 감춤)의 **유일한 예외**다 — "사용자가 부재 사실을 알아야 손해를 피하는 경우"(PHASE 7-1). → SPEC-03 §02 M-05

#### 8. 데이터 / 전달

> **프로토타입의 `09:00 ~ 20:00` 은 어느 결정 문서에도 없다.** 프로토타입 코드(PHASE 7-2 · 7-4)에만 존재한다.
> **`PROTOTYPE-ONLY` / `UNVERIFIED` — 확정 요구사항으로 승격하지 않는다.**
> 실제 가능 시간 값·판정 주체는 `UNVERIFIED` → SPEC-04 DS-5 · DQ-11.

#### 9. 접근성
**비활성 상태도 읽을 수 있어야 한다** — C-06 `disabled`의 명도 대비를 유지한다. "읽히지 않으면 사유 전달이 실패한다"(PHASE 7-1 G) `CONFIRMED`
**색 단독 의존 금지** — C-07 `info` / `caution` 을 색으로만 구분하지 않고 문구·아이콘을 함께 쓴다. `CONFIRMED`

#### 10. 측정
프로토타입 `M05_STATE` 로그는 QA용 `PROTOTYPE-ONLY`. 운영 측정 미확정 → SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`**(구조·인라인 처리·목적지) / **`UNVERIFIED`**(시간 값) |
| 근거 ID | DD-09 · DD-10 · P-A · R-09 · R-10 · UI-P3 |
| 참조 | PHASE 6 M-05 · E · PHASE 7-1 C-06 · C-07 · G |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-29 | 개통 불가 시 처리 | CTA `disabled` + 인라인 문구 · **팝업 없음** | 가능 |
| TC-02-30 | 목적지 경로 | `/onsale/*` 0건 · 현행 경로 사용 | 가능 |
| TC-02-31 | disabled 가독성 | 대비 기준 충족 | 가능 |
| TC-02-32 | 실제 가능 시간 값 | **판정 기준 없음** | **검증 불가 — 값 미확정** |

---

## F-10 · GNB 열기 / 닫기

#### 1. 목적
전체 카테고리 진입 경로를 유지한다. DD-01(로그인 헤더 직접 노출) 적용 후에도 **기존 경로를 끊지 않는다.** (PHASE 7-1 Navigation) `CONFIRMED`

#### 2. 적용 범위
M-11 · C-11 GnbOverlay. **내부 메뉴 트리는 범위 밖.**

#### 3. 선행 조건
상시 노출(헤더 아이콘).

#### 4. 사용자 행동
전체메뉴 아이콘 탭 → 열기. 닫기 버튼 / ESC → 닫기.

#### 5. 시스템 동작

| 동작 | 요건 | 근거 |
|---|---|---|
| 열기 | 오버레이 표시 · **배경 스크롤 잠금** · 포커스를 레이어 안에 가둔다 | PHASE 7-1 G · Navigation `CONFIRMED` |
| 닫기 | 오버레이 해제 · **호출 지점으로 포커스 복귀** | PHASE 7-1 G `CONFIRMED` |
| 상태 전달 | 헤더 버튼의 `aria-expanded` 갱신 | PHASE 7-1 G `CONFIRMED` |

#### 6. 결과
전체 카테고리 + 로그인 진입이 유지된 오버레이.

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 M-11 · §3-3.

#### 8. 데이터 / 전달 — **범위 경계**

| 항목 | 처리 | 상태 |
|---|---|---|
| GNB 내부 메뉴 구조 | **명세하지 않는다** — PHASE 5.2 "주요 메뉴(GNB) **Keep. 변경 없음.** 퀵메뉴 축소가 GNB에 영향 주지 않음" | **`OUT OF SCOPE`** |
| 프로토타입의 GNB 목적지 16개 | 프로토타입에서 처음 확정된 것이며 실제 구조 대조가 없다 | **`PROTOTYPE-ONLY`** |
| 실제 GNB 구조 | 관측되지 않았다 | **`DEVELOPMENT CONFIRMATION REQUIRED`** |
| 2026.01 GNB 개편안과의 관계 | PHASE 1 OPEN-12 — "존재 확인됨 · **관계 미정**" | `OPEN` |

> 내부 구조를 추측해 명세하지 않는다. 확인된 것은 **진입점과 오버레이 동작**뿐이다.

#### 9. 접근성
레이어 포커스 트랩 · 닫으면 호출 지점 복귀 · 열림 중 배경 스크롤 잠금. (PHASE 7-1 G) `CONFIRMED`

#### 10. 측정
전체메뉴 클릭은 현재 `MAIN_NOTLOGIN` 범위 밖(`GNB` 카테고리)이다(PHASE 2). `UNVERIFIED` → SPEC-05.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`CONFIRMED`**(open/close 동작) / **`OUT OF SCOPE`**(내부 구조) |
| 근거 ID | C-11 · DD-01 · OPEN-12 |
| 참조 | PHASE 5.2 D · PHASE 7-1 Navigation · G |

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-33 | 열기/닫기 | 배경 스크롤 잠금 · 포커스 트랩 · ESC · 복귀 | 가능 |
| TC-02-34 | `aria-expanded` 갱신 | 열림 true / 닫힘 false | 가능 |
| TC-02-35 | 내부 메뉴 구조 | **판정 기준 없음** | **검증 불가 — 범위 밖** |

---

## F-11 · 검색

> **이 기능은 확정 기능이 아니다.** 아이콘·레이어 진입과 **검색 실행**을 분리해 기술한다.

#### 1. 목적
검색 진입 경로를 유지한다. **핵심 행동으로 다루지 않는다.** (D-09(5.2) · DD-13)

#### 2. 적용 범위
M-01 헤더 검색 아이콘 · 검색 레이어.

#### 3. 선행 조건
상시 노출(아이콘).

#### 4. 사용자 행동
검색 아이콘 탭 → 레이어 진입. 입력.

#### 5. 시스템 동작

| 항목 | 상태 | 근거 |
|---|---|---|
| 헤더에 **아이콘으로** 둔다. 펼친 입력창을 두지 않는다 | **`CONFIRMED`** | D-09(5.2) · PHASE 7-1 |
| 레이어 진입 · 입력 가능 | `PARTIAL` | PHASE 7-4 |
| **검색 실행** | **`OPEN` — 미구현** | — |
| 최근 검색어 빈 상태 표기 | `PROTOTYPE-ONLY` | PHASE 7-4 |

#### 6. 결과
검색 레이어가 열린다. **실행 결과 화면은 정의되지 않았다.**

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 검색. 검색 결과 없음·오류 처리는 실행 기능이 없으므로 정의되지 않았다 `OPEN`.

#### 8. 데이터 / 전달
정의되지 않음 `OPEN`.

#### 9. 접근성
레이어 포커스 트랩 · ESC · 호출 지점 복귀 · 배경 스크롤 잠금(오버레이 공통). `CONFIRMED`

#### 10. 측정
프로토타입 `SEARCH_OPEN` 로그는 QA용 `PROTOTYPE-ONLY`. 검색 클릭은 현재 `MAIN_NOTLOGIN` 범위 밖(`UTIL_TOP`)이다(PHASE 2).

#### 11. 근거 및 판정 — **DV-02의 결론을 그대로 따른다**

| 항목 | 값 |
|---|---|
| 상태 | **`PARTIAL`**(아이콘·진입) / **`OPEN`**(실행) |
| DV-02 검증 | **종결**(2026-08-26) |
| 그 결론 | **승격 없이 유보 유지** — 아이콘 유지 · **핵심 행동 취급 금지** |
| 판단 근거 | "검색 4,765/1,185 clicks는 요금제찾기와 유사 규모이나 **집계 필터가 세션 범위라 과대추정 가능**" |
| 철회된 근거 | "검색 = 퀵메뉴급"은 교차도구 비율 — **철회 상태 · 재사용 금지** |
| 근거 ID | D-09(5.2) · DD-13 · DV-02 · OI-09 |

> **검증 종결을 검색 기능 확정으로 해석하지 않는다.** 실행 방식·결과 화면·우선순위는 정하지 않는다.

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-36 | 헤더에 아이콘 형태 | 펼친 입력창 0건 | 가능 |
| TC-02-37 | 레이어 진입 | 오버레이 공통 요건 충족 | 가능 |
| TC-02-38 | 검색 실행 | **판정 기준 없음** | **검증 불가 — 미구현** |

---

## F-12 · 팝업

> **구조는 존재하고 규칙은 공백이다.** 이 공백은 누락이 아니라 의도된 상태다.

#### 1. 목적
운영 고지·프로모션을 레이어로 전달한다. (현행 운영 구조)

#### 2. 적용 범위
M-10 · C-10 PopupLayer.

#### 3. 선행 조건
**정의되지 않았다.** `OPEN`

#### 4. 사용자 행동
닫기 3종 — **닫기 / 오늘 하루 그만 보기 / 전체 닫기**. (PHASE 7-1 C-10) `CONFIRMED`

#### 5. 시스템 동작

| 항목 | 내용 | 상태 |
|---|---|---|
| 구조 | 딤 + 콘텐츠 + 닫기 3종 | **`CONFIRMED`** |
| **노출 조건 · 개수 · 동시 노출 상한 · 재노출 주기** | **정의하지 않는다** | **`OPEN`** |
| 자동 노출 | 규칙이 없으므로 명세하지 않는다 | **`OPEN`** |

> PHASE 6 I 전제 승계 — "**규칙이 필요한 화면은 그릴 수 없다.** 구조 설계는 가능하나 규칙은 비워둔다."
> 프로토타입이 팝업을 수동 호출로만 여는 것은 **검증 편의**이며 "자동 노출을 하지 않는다"는 운영 결정이 아니다. `PROTOTYPE-ONLY`

#### 6. 결과
정의되지 않음 `OPEN`.

#### 7. 예외 / 실패 상태
→ SPEC-03 §02 M-10.

#### 8. 데이터 / 전달
팝업 콘텐츠 공급 방식 `OPEN` → SPEC-04 DS-6.

#### 9. 접근성
레이어 열림 시 포커스를 레이어 안에 가두고 닫으면 호출 지점으로 복귀 · 배경 스크롤 잠금 · 닫기 타깃 44×44. **"구조 요건이며 노출 규칙과 무관"**(PHASE 7-1 G) `CONFIRMED`

#### 10. 측정
닫기 3종을 구분해 측정할 수 있어야 한다 — 현행 관측은 팝업 닫기 0.766 / 0.714 회/세션(GA4, F/R)이며 **닫기 유형별 분해가 불가능**하다. 신규 이벤트 필요 여부 → SPEC-05 `UNVERIFIED`.

#### 11. 근거 및 판정

| 항목 | 값 |
|---|---|
| 상태 | **`PARTIAL`**(구조 확정) / **`OPEN`**(규칙 전부) |
| 근거 ID | D-07(5.2)(레이어 분리) · D-07(FR-07)(정책 재설계) · DD-12 · DV-03 · OI-04 · P-01 |
| 참조 | PHASE 6 M-10 · PHASE 7-1 C-10 · G |

> P-01(팝업 닫기가 단일 최대 행동)은 **문제 인식의 근거**이며, 이를 이유로 본 명세에서 노출 규칙을 정하지 않는다. 팝업 정책 재설계는 D-07(FR-07) 결정 사안이다.

#### TC
| ID | 검증 | 기대 | 판정 가능 |
|---|---|---|---|
| TC-02-39 | 닫기 3종 존재 | 닫기 / 오늘 하루 그만 / 전체 닫기 | 가능 |
| TC-02-40 | 레이어 접근성 | 포커스 트랩 · 복귀 · 스크롤 잠금 | 가능 |
| TC-02-41 | 자동 노출 규칙 | **판정 기준 없음** | **검증 불가 — 규칙 미확정** |

---

## 13. Prototype-only 경계

**PROTOTYPE-ONLY — 개발 요구사항으로 간주하지 않음**

| 항목 | 비고 |
|---|---|
| 인터랙션 로그 21종 (`INIT` · `RANGE_SELECT` · `HANDOFF` · `NAVIGATE` · `BACK` · `M03_STATE` · `M03_RETRY` · `M04_STATE` · `M05_STATE` · `PHONE_TOGGLE` · `PHONE_TAB` · `ACC_TOGGLE` · `CAROUSEL` · `SEARCH_OPEN` · `ATF_GUIDE` · `ANNOTATE` · `AUDIT_OK` · `AUDIT_FAIL` · `BLOCKED` · `RESET` · `RESIZE`) | **QA·개발 검증용 콘솔 로그. GA4/GTM 운영 이벤트로 간주하지 않는다.** 운영 이벤트는 SPEC-05에서 별도 정의 |
| 상태 객체 `S` · 메모리 전용 보관 | 검증 구현 |
| pane 내부 전환 (`paneMain` / `panePric` / `paneExt`) | 실제는 페이지 이동 |
| SAMPLE 데이터 (`PLANS` 6 · `SLIDES` 3 · `PHONES` 12) | 구조 검증용 · 실데이터 아님 |
| 검증 도구 `drawer` / `devToggle` · 모듈 주석 · AtF 가이드 · 폭 전환 · 터치 감사 · 초기화 | 검증 장치 |
| browser history 미구현 | 구현 부재이며 요구 부재가 아니다 |
| 검색 실행 미구현 | 동일 |
| 팝업 자동 노출 규칙 미구현 | 동일 |
| 셀프개통 `09:00 ~ 20:00` | 결정 문서에 없음 |
| 오류·빈 상태 문구 전체 | 카피 미확정 |
| 임계값·전환 시간 (`scrollTop > 4`, 캐러셀 주기 등) | 구현 세부 |
| GNB 목적지 16개 | 실제 구조 미대조 |
| 토스트 안내(`BLOCKED` 시) | 프로토타입 피드백 장치 |

---

## 부록 A. TC 목록

| TC ID | F-ID | 대상 | 판정 |
|---|---|---|---|
| TC-02-01 ~ 03 | F-01 | QuickMenu 3항목·순서·목적지·이벤트 부재 | 가능 |
| TC-02-04 ~ 07 | F-02 | 5행 동시 노출 · 단일 선택 · 접근가능 이름 · 금지 요소 부재 | 가능 |
| TC-02-08 ~ 11 | F-03 | `dan2` 생성 · 파싱 · 선적용(현 상태) · 정렬(현 상태) | 가능 |
| TC-02-12 | F-03 | 연결 후 선적용 | **검증 불가 — 개발 확인 필요** |
| TC-02-13 ~ 14 | F-04 | 목적지 경로 · `pplrPpn` 직결 부재 | 가능 |
| TC-02-15 ~ 16 | F-05 | 스크롤 복원 · 접힘 유지 | **부분 — 수단 미확정** |
| TC-02-17 ~ 18 | F-05 | 재진입 유지 · 새로고침 후 조건 유지 | **검증 불가 — 개발 확인 필요** |
| TC-02-19 ~ 20 | F-06 | 초기 접힘 · 펼칠 때 로드 | 가능 |
| TC-02-21 | F-06 | 펼침 실패 | **검증 불가 — 개발 확인 필요** |
| TC-02-22 ~ 23 | F-07 | 3탭 ARIA · 키보드 전환 | 가능 |
| TC-02-24 | F-07 | 탭 라벨 | **검증 불가 — 라벨 미확정** |
| TC-02-25 ~ 28 | F-08 | 정지 컨트롤 · reduced-motion · 위치 텍스트 · 0건 미노출 | 가능 |
| TC-02-29 ~ 31 | F-09 | 개통 불가 인라인 · 목적지 · disabled 가독성 | 가능 |
| TC-02-32 | F-09 | 실제 가능 시간 값 | **검증 불가 — 값 미확정** |
| TC-02-33 ~ 34 | F-10 | 오버레이 동작 · `aria-expanded` | 가능 |
| TC-02-35 | F-10 | 내부 메뉴 구조 | **검증 불가 — 범위 밖** |
| TC-02-36 ~ 37 | F-11 | 아이콘 형태 · 레이어 진입 | 가능 |
| TC-02-38 | F-11 | 검색 실행 | **검증 불가 — 미구현** |
| TC-02-39 ~ 40 | F-12 | 닫기 3종 · 레이어 접근성 | 가능 |
| TC-02-41 | F-12 | 자동 노출 규칙 | **검증 불가 — 규칙 미확정** |

**총 41건 — 판정 가능 31 · 부분 2 · 검증 불가 8**

검증할 수 없는 항목을 PASS 조건으로 만들지 않았다.

---

## 부록 B. 미확정 이월

원자료를 재대조해 **실제 상태**로 기록한다. 기존 ID를 변경하지 않는다.

| ID | 항목 | 상태 | 관련 F |
|---|---|---|---|
| **TR-03**(표기 A) | 뒤로가기·새로고침 후 조건 유지 | `UNVERIFIED` | F-05 |
| **TR-03**(표기 B) | 목록→메인 복귀 시 선택 구간 유지 | `UNVERIFIED` · 현 단계 범위 밖 | F-05 |
| **DV-04** | 분모 기간 정합 | **`UNVERIFIED` — 문서 간 상태 상충, 분석 확인 필요**<br><span>2026-08-27 정정 — 본 문서는 PHASE 7-4 이월 블록의 "해소" 표기만 반영해 이월 제외로 적었으나, PHASE 5.1 파생 비율 검증에 **S1(2026-07-28~08-24) ÷ S2(2026-07-29~08-25)의 1일 기간 오차가 잔존**한다. 어느 쪽이 맞는지 원자료만으로 확정할 수 없어 이월로 되돌린다 — SPEC-05 §07 · SPEC_CROSS_QA C-1 · 결정 요청 B-30</span> | — (측정 사안) |
| **TR-04** | 파싱 결과를 초기화 로직에 연결 | `OPEN` · 개발 확인 | F-03 |
| **DV-01** | `#phoneArea` 측정 태깅 4종 | `UNVERIFIED` · 태깅 전 확대 금지 | F-06 · F-07 |
| **DV-03** | 팝업 개수·노출 규칙·재노출 주기 | `OPEN` · 운영/CMS | F-12 |
| **DC-03** | M-07 운영 슬롯 규칙 | `OPEN` · 제휴영업팀 | — (M-07은 F-ID 없음) |
| **DC-04** | 목적지 화면 주체(기존/신규) | `OPEN` · 요청자 확정 | F-01 · F-04 · F-09 |
| **OI-06** | 대표 가격 소스·갱신 주기·정의(최저가/중위가) | `UNVERIFIED` | F-02 |
| **OI-07** | sticky 헤더 세로 공간 부작용 | `UNVERIFIED` · 실측 | — (SPEC-01 소관) |
| **RS-01** | 복귀 목적 · 인지 수단 · `joinComplete` 내용 | `UNVERIFIED` / `OPEN` / `CONDITIONAL` · **화면 변형 0건** | — (F-ID 없음 · SPEC-03 §4-2) |
| — | M-03 `조회 실패`와 "P0는 항상 렌더" 원칙의 관계 | `UNVERIFIED` | F-02 |
| — | M-06 펼침 실패 동작 | **`SPEC GAP`** · 원칙만 확정 | F-06 |
| — | M-06 탭 라벨 (문서 간 상이) | `UNVERIFIED` | F-07 |
| — | 앱 WebView 진입 처리 | `OPEN` · `SPEC GAP` | — (SPEC-03 §4-3) |
| — | 로그인 오류 복귀 처리 | `OPEN` · `SPEC GAP` | — (SPEC-03 §4-4) |
| — | 전역 네트워크 오류 감지·복구 전이 | **`SPEC GAP`** · 원칙만 확정 | — (SPEC-03 §3-1) |
| — | 셀프개통 가능 시간 값·판정 주체 | `UNVERIFIED` · `PROTOTYPE-ONLY` | F-09 |
| — | 정렬 초기값 지정 파라미터 존재 여부 | `UNVERIFIED` | F-03 |
| — | 실제 GNB 구조 | `UNVERIFIED` · `DEVELOPMENT CONFIRMATION REQUIRED` | F-10 |
| — | 배너 개수 상한·정렬 규칙 | `UNVERIFIED` | F-08 |

**해소되어 이월하지 않는 항목 (재대조 결과)**

| ID | 상태 | 사유 |
|---|---|---|
| **TR-01** | 해소 | 실측으로 "선적용되지 않음" 확정. 사실로 기록됨(F-03) |
| **TR-02** | 해소 | 수단 = 쿼리스트링 · 파서 존재 확정 |
| **DV-02** | 검증 종결 | 결론 = 승격 없이 유보 유지. **기능 확정이 아님**(F-11) |
| **OI-01** | 해소 표기 | 단 채택 브랜드값은 `UNVERIFIED` (SPEC-01 소관) |

**본 명세에서 새로 만든 것**

| 항목 | 성격 |
|---|---|
| `TC-02-01` ~ `TC-02-41` | 검증 항목 ID (신규) |

`F-01` ~ `F-12`는 지정된 정본이며 추가·삭제하지 않았다. 새로운 D/DD/DV/OI/DC/TR/RS/ME ID는 만들지 않았다.

---

## SPEC-03 · SPEC-04와의 정합

| 항목 | SPEC-02 | SPEC-03 | SPEC-04 | 충돌 |
|---|---|---|---|---|
| M-03 예외 | 진입점만 | 상태 상세 | 데이터 계약 | 없음 |
| 조건 전달 | 기능 흐름 | — | 파라미터 사실 | 없음 |
| TR-03A/B | 표기 동일 | 표기 동일 | 표기 동일 | 없음 |
| 상태 지속 | 요구 확정 · 수단 미확정 | 동일(§3-4) | 수단 `UNVERIFIED` | 없음 |
| 셀프개통 시간 | `PROTOTYPE-ONLY` | `PROTOTYPE-ONLY` | DS-5 `UNVERIFIED` | 없음 |
| 탭 라벨 | `UNVERIFIED` | `UNVERIFIED` | — | 없음 |
| 팝업 규칙 | `OPEN` | `OPEN` | DS-6 `OPEN` | 없음 |
| 검색 | `PARTIAL`/`OPEN` | 동일 | — | 없음 |
| SAMPLE 경계 | §13 | §07 | §05 | 없음 |

---

기준 · PHASE 5.1 · PHASE 5.2 · PHASE 6(Design Decision · E · F · STATE) · PHASE 7-1(C · D · E · F · G · Navigation) · PHASE 7-2 / 7-3 / 7-4 Prototype · PHASE 2 현행 DOM 관측 · SPEC-03 · SPEC-04 · SPEC_PREFLIGHT · PHASE9_SPEC_READINESS
본 명세는 확정된 결정과 프로토타입에서 확인된 동작만을 기술한다. 미확정 사항은 확정형으로 바꾸지 않았고, 근거 없는 구현 방식을 추가하지 않았다.
